# Product Requirements Document (PRD)
# نظام الشراء الجماعي - Group Buying System

---

## 📋 معلومات المستند

| البند | القيمة |
|-------|--------|
| **رقم المستند** | PRD-HADEROS-GB-001 |
| **الإصدار** | 1.0 |
| **تاريخ الإنشاء** | 2 يناير 2026 |
| **الحالة** | مسودة للمراجعة |
| **المؤلف** | فريق HADEROS |
| **المراجعون** | - |

---

## 1. نظرة عامة (Overview)

### 1.1 ملخص تنفيذي
نظام الشراء الجماعي هو ميزة تتيح للمستخدمين تشكيل مجموعات للشراء معاً والحصول على خصومات تتناسب طردياً مع حجم المجموعة. كلما زاد عدد المشاركين، انخفض السعر للجميع.

### 1.2 الرؤية
"تمكين المستهلك المصري من الحصول على أسعار الجملة من خلال قوة الشراء الجماعي"

### 1.3 الأهداف الاستراتيجية

| الهدف | المقياس | المستهدف |
|-------|---------|----------|
| زيادة حجم الطلبات | متوسط حجم الطلب | +40% |
| اكتساب عملاء جدد | عملاء جدد شهرياً | +25% |
| تحسين الانتشار الفيروسي | معدل المشاركة | 3x لكل عرض |
| زيادة الإيرادات | إيرادات Group Buying | 15% من إجمالي الإيرادات |

### 1.4 الجمهور المستهدف

| الشريحة | الوصف | الاحتياجات |
|---------|-------|-----------|
| **العائلات** | عائلات تشتري معاً | توفير، منتجات منزلية |
| **زملاء العمل** | موظفين في نفس الشركة | إلكترونيات، أدوات مكتبية |
| **الجيران** | سكان نفس العمارة/الحي | مستلزمات يومية |
| **مجموعات WhatsApp** | أصدقاء، معارف | منتجات متنوعة |
| **الأمهات** | أمهات المدارس | مستلزمات مدرسية، ملابس أطفال |

---

## 2. المتطلبات الوظيفية (Functional Requirements)

### 2.1 إدارة العروض الجماعية

#### FR-GB-001: إنشاء عرض جماعي جديد
**الوصف:** يتيح للبائع/التاجر إنشاء عرض جماعي على منتج معين

**المدخلات:**
| الحقل | النوع | مطلوب | الوصف |
|-------|-------|-------|-------|
| product_id | Integer | نعم | المنتج المعروض |
| title | String(255) | نعم | عنوان العرض بالإنجليزية |
| title_ar | String(255) | نعم | عنوان العرض بالعربية |
| description | Text | لا | وصف تفصيلي |
| description_ar | Text | لا | وصف تفصيلي بالعربية |
| original_price | Decimal | نعم | السعر الأصلي |
| min_participants | Integer | نعم | الحد الأدنى للتفعيل |
| max_participants | Integer | لا | الحد الأقصى |
| start_time | DateTime | نعم | وقت بدء العرض |
| end_time | DateTime | نعم | وقت انتهاء العرض |
| tiers | Array | نعم | مستويات الأسعار |
| require_payment_upfront | Boolean | نعم | الدفع مقدماً أم لا |

**مستويات الأسعار (Tiers):**
```json
{
  "tiers": [
    { "min": 5, "max": 10, "price": 180, "name": "Bronze" },
    { "min": 11, "max": 20, "price": 160, "name": "Silver" },
    { "min": 21, "max": 50, "price": 140, "name": "Gold" },
    { "min": 51, "max": null, "price": 120, "name": "Platinum" }
  ]
}
```

**المخرجات:**
- كود العرض الفريد (deal_code)
- رابط المشاركة
- QR Code

**قواعد العمل:**
- BR-001: يجب أن يكون min_participants >= 2
- BR-002: يجب أن يكون end_time > start_time
- BR-003: يجب أن يكون min_price <= original_price
- BR-004: يجب موافقة KAIA على العرض قبل النشر
- BR-005: الحد الأقصى لمدة العرض 30 يوم

---

#### FR-GB-002: نظام مستويات الأسعار (Tier Pricing)
**الوصف:** نظام ديناميكي لحساب السعر بناءً على عدد المشاركين

**خوارزمية الحساب:**
```typescript
function calculateCurrentPrice(deal: GroupDeal): PriceResult {
  const participants = deal.currentParticipants;

  // البحث عن المستوى المناسب
  const tier = deal.tiers
    .sort((a, b) => b.minParticipants - a.minParticipants)
    .find(t => participants >= t.minParticipants);

  if (!tier) {
    return {
      price: deal.originalPrice,
      tier: null,
      discount: 0,
      nextTier: deal.tiers[0]
    };
  }

  // حساب المستوى التالي
  const nextTier = deal.tiers.find(t => t.minParticipants > participants);

  return {
    price: tier.price,
    tier: tier,
    discount: ((deal.originalPrice - tier.price) / deal.originalPrice) * 100,
    nextTier: nextTier,
    participantsToNextTier: nextTier ? nextTier.minParticipants - participants : null
  };
}
```

**عرض التقدم للمستخدم:**
```
┌─────────────────────────────────────────────────────────┐
│  السعر الأصلي: ~~200 ج.م~~                              │
│  السعر الحالي: 160 ج.م (خصم 20%)                        │
│                                                         │
│  [████████████░░░░░░░░] 12/20 مشارك                    │
│                                                         │
│  🎯 باقي 8 أشخاص للوصول للمستوى الذهبي (140 ج.م)!      │
└─────────────────────────────────────────────────────────┘
```

---

#### FR-GB-003: الانضمام للعرض الجماعي
**الوصف:** يتيح للمستخدم الانضمام لعرض جماعي قائم

**المدخلات:**
| الحقل | النوع | مطلوب | الوصف |
|-------|-------|-------|-------|
| deal_id | Integer | نعم | رقم العرض |
| participant_name | String | نعم | اسم المشارك |
| participant_phone | String | نعم | رقم الهاتف |
| participant_email | String | لا | البريد الإلكتروني |
| quantity | Integer | نعم | الكمية المطلوبة |
| referral_code | String | لا | كود الإحالة |

**التحققات:**
```typescript
async function validateJoinRequest(request: JoinRequest): Promise<ValidationResult> {
  const errors = [];

  // التحقق من حالة العرض
  if (deal.status !== 'active') {
    errors.push('العرض غير نشط');
  }

  // التحقق من الوقت
  if (new Date() > deal.endTime) {
    errors.push('انتهى وقت العرض');
  }

  // التحقق من الحد الأقصى
  if (deal.maxParticipants && deal.currentParticipants >= deal.maxParticipants) {
    errors.push('تم الوصول للحد الأقصى من المشاركين');
  }

  // التحقق من المخزون
  const totalRequestedQuantity = deal.participants.reduce((sum, p) => sum + p.quantity, 0) + request.quantity;
  if (totalRequestedQuantity > product.stock) {
    errors.push('الكمية المطلوبة غير متوفرة');
  }

  // التحقق من عدم التكرار
  const existingParticipant = await findParticipantByPhone(deal.id, request.phone);
  if (existingParticipant) {
    errors.push('أنت مشارك بالفعل في هذا العرض');
  }

  return { valid: errors.length === 0, errors };
}
```

**السيناريوهات:**

| السيناريو | الإجراء |
|-----------|---------|
| الدفع المقدم مطلوب | توجيه لصفحة الدفع فوراً |
| الدفع عند التأكيد | حجز مكان + انتظار نجاح العرض |
| العرض ممتلئ | إضافة لقائمة الانتظار |
| انتهاء الوقت | رفض الطلب |

---

#### FR-GB-004: مشاركة العرض
**الوصف:** نظام مشاركة العرض عبر القنوات المختلفة

**قنوات المشاركة:**

**1. WhatsApp:**
```typescript
function generateWhatsAppMessage(deal: GroupDeal, shareCode: string): string {
  const currentPrice = calculateCurrentPrice(deal);
  const savings = deal.originalPrice - currentPrice.price;
  const progress = (deal.currentParticipants / deal.minParticipants) * 100;

  return `
🛒 *عرض جماعي لا يفوتك!*

📦 *${deal.titleAr}*

💰 السعر الأصلي: ~${deal.originalPrice} ج.م~
🔥 السعر الحالي: *${currentPrice.price} ج.م*
💵 توفيرك: ${savings} ج.م (${currentPrice.discount.toFixed(0)}%)

👥 المشاركين: ${deal.currentParticipants}/${deal.minParticipants}
📊 التقدم: ${progress.toFixed(0)}%

${currentPrice.nextTier ? `
🎯 *باقي ${currentPrice.participantsToNextTier} أشخاص فقط للوصول لـ ${currentPrice.nextTier.price} ج.م!*
` : ''}

⏰ ينتهي: ${formatTimeRemaining(deal.endTime)}

👇 انضم الآن:
https://haderos.com/gd/${deal.dealCode}?ref=${shareCode}

كل ما نزيد، السعر ينزل! 📉
  `.trim();
}

function generateWhatsAppLink(message: string): string {
  return `https://wa.me/?text=${encodeURIComponent(message)}`;
}
```

**2. Facebook:**
```typescript
function generateFacebookShareLink(deal: GroupDeal): string {
  const url = `https://haderos.com/gd/${deal.dealCode}`;
  const quote = `انضم معانا! ${deal.titleAr} بخصم يصل إلى ${maxDiscount}%`;
  return `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}&quote=${encodeURIComponent(quote)}`;
}
```

**3. نسخ الرابط:**
```typescript
function generateShareableLink(deal: GroupDeal, shareCode: string): ShareLink {
  return {
    url: `https://haderos.com/gd/${deal.dealCode}?ref=${shareCode}`,
    shortUrl: `https://hd.rs/g/${deal.dealCode}`,
    qrCode: generateQRCode(`https://haderos.com/gd/${deal.dealCode}?ref=${shareCode}`)
  };
}
```

**تتبع المشاركات:**
```sql
-- عند كل مشاركة
INSERT INTO group_deal_shares (deal_id, shared_by, share_platform, share_code)
VALUES (?, ?, ?, ?);

-- عند كل نقرة على الرابط
UPDATE group_deal_shares SET clicks = clicks + 1 WHERE share_code = ?;

-- عند كل تحويل (انضمام)
UPDATE group_deal_shares SET conversions = conversions + 1 WHERE share_code = ?;
```

---

#### FR-GB-005: إغلاق العرض
**الوصف:** نظام إغلاق تلقائي للعرض عند انتهاء الوقت أو اكتمال العدد

**سيناريوهات الإغلاق:**

**1. نجاح العرض (تم الوصول للحد الأدنى):**
```typescript
async function handleDealSuccess(dealId: number): Promise<void> {
  const deal = await getDeal(dealId);

  // 1. تحديث حالة العرض
  await updateDealStatus(dealId, 'success');

  // 2. حساب السعر النهائي
  const finalPrice = calculateCurrentPrice(deal).price;

  // 3. تحديث السعر النهائي لكل المشاركين
  await updateAllParticipantsFinalPrice(dealId, finalPrice);

  // 4. إنشاء طلبات للمشاركين
  for (const participant of deal.participants) {
    const order = await createOrder({
      customerId: participant.userId,
      customerName: participant.participantName,
      customerPhone: participant.participantPhone,
      items: [{
        productId: deal.productId,
        quantity: participant.quantity,
        price: finalPrice,
        originalPrice: deal.originalPrice
      }],
      source: 'group_deal',
      sourceId: dealId,
      paymentStatus: participant.paymentStatus === 'paid' ? 'paid' : 'pending'
    });

    participant.orderId = order.id;
  }

  // 5. إرسال إشعارات النجاح
  await sendSuccessNotifications(dealId, finalPrice);

  // 6. تحديث المخزون
  const totalQuantity = deal.participants.reduce((sum, p) => sum + p.quantity, 0);
  await decrementStock(deal.productId, totalQuantity);
}
```

**2. فشل العرض (لم يتم الوصول للحد الأدنى):**
```typescript
async function handleDealFailure(dealId: number): Promise<void> {
  const deal = await getDeal(dealId);

  // 1. تحديث حالة العرض
  await updateDealStatus(dealId, 'failed');

  // 2. استرداد المبالغ للمشاركين الذين دفعوا
  for (const participant of deal.participants) {
    if (participant.paymentStatus === 'paid') {
      await processRefund({
        participantId: participant.id,
        amount: participant.lockedPrice * participant.quantity,
        reason: 'group_deal_failed'
      });
    }
  }

  // 3. إرسال إشعارات الفشل
  await sendFailureNotifications(dealId);
}
```

**3. إغلاق مبكر (اكتمال الحد الأقصى):**
```typescript
async function checkEarlyClose(dealId: number): Promise<void> {
  const deal = await getDeal(dealId);

  if (deal.autoCloseOnMax && deal.currentParticipants >= deal.maxParticipants) {
    await handleDealSuccess(dealId);
  }
}
```

---

#### FR-GB-006: نظام الإشعارات
**الوصف:** إشعارات real-time للمشاركين حول تقدم العرض

**أنواع الإشعارات:**

| النوع | التوقيت | المحتوى |
|-------|---------|---------|
| milestone_25 | 25% من الهدف | "وصلنا ربع الطريق! باقي X أشخاص" |
| milestone_50 | 50% من الهدف | "نصف الطريق! الحماس يزيد 🔥" |
| milestone_75 | 75% من الهدف | "قربنا جداً! باقي X أشخاص فقط" |
| milestone_90 | 90% من الهدف | "آخر X أشخاص للنجاح! 🎯" |
| price_drop | انخفاض السعر | "السعر نزل! أصبح X ج.م بدلاً من Y ج.م" |
| new_tier | الوصول لمستوى جديد | "🎉 وصلنا المستوى الفضي!" |
| ending_soon_24h | 24 ساعة للانتهاء | "⏰ باقي 24 ساعة فقط!" |
| ending_soon_1h | ساعة للانتهاء | "⚡ آخر ساعة! انضم الآن" |
| success | نجاح العرض | "🎉 مبروك! العرض نجح" |
| failed | فشل العرض | "للأسف لم يكتمل العدد" |

**قنوات الإشعارات:**
```typescript
async function sendNotification(dealId: number, type: NotificationType): Promise<void> {
  const deal = await getDeal(dealId);
  const message = generateNotificationMessage(deal, type);

  for (const participant of deal.participants) {
    // 1. Push Notification
    await sendPushNotification(participant.userId, message);

    // 2. SMS (للإشعارات المهمة فقط)
    if (['success', 'failed', 'ending_soon_1h'].includes(type)) {
      await sendSMS(participant.participantPhone, message.sms);
    }

    // 3. WhatsApp (إذا متاح)
    if (participant.whatsappOptIn) {
      await sendWhatsAppMessage(participant.participantPhone, message.whatsapp);
    }

    // 4. In-App Notification
    await createInAppNotification(participant.userId, message);
  }

  // تسجيل الإشعار
  await logNotification(dealId, type, message);
}
```

---

#### FR-GB-007: استرداد المبالغ
**الوصف:** نظام استرداد المبالغ عند فشل العرض أو إلغاء المشاركة

**سياسة الاسترداد:**

| الحالة | الاسترداد | المدة |
|--------|----------|-------|
| فشل العرض | 100% | 3-5 أيام عمل |
| إلغاء قبل 24 ساعة من الانتهاء | 100% | 3-5 أيام عمل |
| إلغاء آخر 24 ساعة | 90% (10% رسوم) | 3-5 أيام عمل |
| بعد نجاح العرض | لا يمكن الإلغاء | - |

```typescript
async function processRefund(request: RefundRequest): Promise<RefundResult> {
  const participant = await getParticipant(request.participantId);

  // حساب المبلغ المسترد
  let refundAmount = participant.lockedPrice * participant.quantity;
  let refundPercentage = 100;

  if (request.reason === 'participant_cancelled') {
    const hoursRemaining = getHoursRemaining(participant.deal.endTime);
    if (hoursRemaining < 24) {
      refundPercentage = 90;
      refundAmount = refundAmount * 0.9;
    }
  }

  // تنفيذ الاسترداد حسب طريقة الدفع
  let refundResult;
  switch (participant.paymentMethod) {
    case 'card':
      refundResult = await refundToCard(participant.paymentReference, refundAmount);
      break;
    case 'wallet':
      refundResult = await refundToWallet(participant.userId, refundAmount);
      break;
    case 'instapay':
      refundResult = await refundToInstaPay(participant.participantPhone, refundAmount);
      break;
  }

  // تحديث حالة المشارك
  await updateParticipantStatus(participant.id, {
    paymentStatus: 'refunded',
    refundAmount: refundAmount,
    refundedAt: new Date()
  });

  return refundResult;
}
```

---

### 2.2 واجهات المستخدم

#### UI-GB-001: صفحة قائمة العروض الجماعية
```
┌─────────────────────────────────────────────────────────────────┐
│  🛒 العروض الجماعية                              [🔍 بحث]      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [تصفية: الكل ▼]  [ترتيب: الأكثر شعبية ▼]                       │
│                                                                 │
│  ┌─────────────────────┐  ┌─────────────────────┐               │
│  │ [صورة المنتج]       │  │ [صورة المنتج]       │               │
│  │                     │  │                     │               │
│  │ iPhone 15 Pro       │  │ Samsung TV 55"      │               │
│  │ ~~50,000~~ 42,000   │  │ ~~25,000~~ 20,000   │               │
│  │ خصم 16%            │  │ خصم 20%            │               │
│  │                     │  │                     │               │
│  │ [████████░░] 80%    │  │ [██████░░░░] 60%    │               │
│  │ 40/50 مشارك        │  │ 30/50 مشارك        │               │
│  │                     │  │                     │               │
│  │ ⏰ ينتهي خلال 2 يوم │  │ ⏰ ينتهي خلال 5 أيام│               │
│  │                     │  │                     │               │
│  │ [  انضم الآن  ]     │  │ [  انضم الآن  ]     │               │
│  └─────────────────────┘  └─────────────────────┘               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### UI-GB-002: صفحة تفاصيل العرض
```
┌─────────────────────────────────────────────────────────────────┐
│  ← رجوع                                         [♡] [📤]        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │                    [صورة المنتج]                        │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  iPhone 15 Pro Max 256GB                                        │
│  ───────────────────────────────────────────────────────────   │
│                                                                 │
│  💰 السعر                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  السعر الأصلي:  ~~50,000 ج.م~~                          │   │
│  │  السعر الحالي:  42,000 ج.م                              │   │
│  │  توفيرك:        8,000 ج.م (16%)                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  👥 التقدم                                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  [████████████████░░░░░░░░░░░░] 40/50 مشارك             │   │
│  │                                                         │   │
│  │  🎯 باقي 10 أشخاص للوصول لـ 40,000 ج.م!                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📊 مستويات الأسعار                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ○ برونزي (5-20):   45,000 ج.م  ✓ تم                    │   │
│  │  ● فضي (21-40):     42,000 ج.م  ← أنت هنا               │   │
│  │  ○ ذهبي (41-60):    40,000 ج.م  باقي 10                 │   │
│  │  ○ بلاتيني (61+):   38,000 ج.م  باقي 30                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⏰ ينتهي العرض خلال                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           02        :        15        :        30      │   │
│  │          أيام             ساعات            دقائق        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📝 وصف المنتج                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  iPhone 15 Pro Max بمعالج A17 Pro...                    │   │
│  │  [عرض المزيد]                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  👥 آخر المنضمين                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  [👤] أحمد م. - منذ 5 دقائق                             │   │
│  │  [👤] سارة ع. - منذ 12 دقيقة                            │   │
│  │  [👤] محمد ح. - منذ 25 دقيقة                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           [     انضم الآن - 42,000 ج.م     ]            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │ 📱 WhatsApp   │  │ 📘 Facebook   │  │ 🔗 نسخ الرابط │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### UI-GB-003: نموذج الانضمام
```
┌─────────────────────────────────────────────────────────────────┐
│  ← رجوع              انضم للعرض                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📦 iPhone 15 Pro Max 256GB                                     │
│  💰 42,000 ج.م (خصم 16%)                                        │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  الكمية                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │    [ - ]           1           [ + ]                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  الاسم الكامل *                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  أحمد محمد علي                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  رقم الهاتف (WhatsApp) *                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  +20  │  01012345678                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  البريد الإلكتروني                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ahmed@example.com                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  كود الإحالة (اختياري)                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ABC123                                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  ملخص الطلب                                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  المنتج:        iPhone 15 Pro Max                       │   │
│  │  الكمية:        1                                       │   │
│  │  السعر:         42,000 ج.م                              │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  الإجمالي:      42,000 ج.م                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⚠️ ملاحظة: السعر النهائي يعتمد على عدد المشاركين عند           │
│     انتهاء العرض. قد ينخفض أكثر!                                │
│                                                                 │
│  ☑️ أوافق على الشروط والأحكام                                   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │           [     تأكيد والدفع     ]                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. المتطلبات غير الوظيفية (Non-Functional Requirements)

### 3.1 الأداء (Performance)
| المتطلب | القيمة |
|---------|--------|
| زمن تحميل صفحة العرض | < 2 ثانية |
| زمن تحديث التقدم | Real-time (< 500ms) |
| زمن معالجة الانضمام | < 3 ثواني |
| الطلبات المتزامنة | 1000+ طلب/ثانية |

### 3.2 الأمان (Security)
- تشفير HTTPS لكل الطلبات
- التحقق من الهوية للعمليات الحساسة
- حماية من CSRF
- Rate limiting للـ API
- تشفير بيانات الدفع (PCI DSS)

### 3.3 التوفر (Availability)
- Uptime: 99.9%
- Failover تلقائي
- Backup كل 6 ساعات

### 3.4 التوسع (Scalability)
- دعم 100,000+ عرض نشط
- دعم 1,000,000+ مشارك متزامن
- Horizontal scaling للخوادم

---

## 4. قاعدة البيانات (Database Schema)

### 4.1 Entity Relationship Diagram

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│   products       │     │   group_deals    │     │   users          │
├──────────────────┤     ├──────────────────┤     ├──────────────────┤
│ id (PK)          │◄────│ product_id (FK)  │     │ id (PK)          │
│ name             │     │ id (PK)          │     │ name             │
│ price            │     │ deal_code        │────►│ email            │
│ stock            │     │ title            │     │ phone            │
└──────────────────┘     │ original_price   │     └──────────────────┘
                         │ min_participants │              │
                         │ max_participants │              │
                         │ current_participants           │
                         │ status           │              │
                         │ start_time       │              │
                         │ end_time         │              │
                         │ created_by (FK)  │──────────────┘
                         └────────┬─────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          │                       │                       │
          ▼                       ▼                       ▼
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ group_deal_tiers │     │ group_deal_      │     │ group_deal_      │
├──────────────────┤     │ participants     │     │ shares           │
│ id (PK)          │     ├──────────────────┤     ├──────────────────┤
│ deal_id (FK)     │     │ id (PK)          │     │ id (PK)          │
│ min_participants │     │ deal_id (FK)     │     │ deal_id (FK)     │
│ max_participants │     │ user_id (FK)     │     │ shared_by (FK)   │
│ price            │     │ participant_name │     │ share_platform   │
│ discount_percent │     │ participant_phone│     │ share_code       │
│ tier_name        │     │ quantity         │     │ clicks           │
└──────────────────┘     │ locked_price     │     │ conversions      │
                         │ final_price      │     └──────────────────┘
                         │ payment_status   │
                         │ referred_by (FK) │──┐
                         └──────────────────┘  │
                                  │            │
                                  └────────────┘
```

### 4.2 DDL Scripts

```sql
-- =====================================================
-- Group Buying System Database Schema
-- =====================================================

-- 1. العروض الجماعية
CREATE TABLE group_deals (
    id INT AUTO_INCREMENT PRIMARY KEY,
    deal_code VARCHAR(20) NOT NULL UNIQUE,
    product_id INT NOT NULL,

    -- المعلومات الأساسية
    title VARCHAR(255) NOT NULL,
    title_ar VARCHAR(255) NOT NULL,
    description TEXT,
    description_ar TEXT,

    -- التسعير
    original_price DECIMAL(10,2) NOT NULL,
    min_price DECIMAL(10,2) NOT NULL,

    -- المشاركين
    min_participants INT NOT NULL DEFAULT 5,
    max_participants INT,
    current_participants INT DEFAULT 0,

    -- التوقيت
    start_time DATETIME NOT NULL,
    end_time DATETIME NOT NULL,

    -- الحالة
    status ENUM('draft', 'pending_approval', 'active', 'success', 'failed', 'cancelled')
        DEFAULT 'draft',

    -- الإعدادات
    auto_close_on_max BOOLEAN DEFAULT TRUE,
    allow_overfill BOOLEAN DEFAULT FALSE,
    require_payment_upfront BOOLEAN DEFAULT TRUE,

    -- KAIA
    kaia_approved BOOLEAN DEFAULT FALSE,
    kaia_approval_date DATETIME,
    kaia_notes TEXT,

    -- الصور
    image_url VARCHAR(500),

    -- الإحصائيات
    total_views INT DEFAULT 0,
    total_shares INT DEFAULT 0,

    -- المنشئ
    created_by INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    -- الفهارس
    INDEX idx_status (status),
    INDEX idx_end_time (end_time),
    INDEX idx_product (product_id),
    INDEX idx_deal_code (deal_code),

    -- المفاتيح الخارجية
    FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE RESTRICT,
    FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 2. مستويات الأسعار
CREATE TABLE group_deal_tiers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    deal_id INT NOT NULL,

    min_participants INT NOT NULL,
    max_participants INT,
    price DECIMAL(10,2) NOT NULL,
    discount_percentage DECIMAL(5,2),

    tier_name VARCHAR(50),
    tier_name_ar VARCHAR(50),
    tier_color VARCHAR(7), -- Hex color
    tier_icon VARCHAR(10), -- Emoji

    display_order INT DEFAULT 0,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_deal (deal_id),

    FOREIGN KEY (deal_id) REFERENCES group_deals(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 3. المشاركين
CREATE TABLE group_deal_participants (
    id INT AUTO_INCREMENT PRIMARY KEY,
    deal_id INT NOT NULL,
    user_id INT,

    -- معلومات المشارك
    participant_name VARCHAR(100) NOT NULL,
    participant_phone VARCHAR(20) NOT NULL,
    participant_email VARCHAR(255),

    -- الطلب
    quantity INT DEFAULT 1,
    locked_price DECIMAL(10,2),
    final_price DECIMAL(10,2),

    -- الدفع
    payment_status ENUM('pending', 'paid', 'refunded', 'failed') DEFAULT 'pending',
    payment_method VARCHAR(50),
    payment_reference VARCHAR(100),
    paid_at DATETIME,
    refunded_at DATETIME,
    refund_amount DECIMAL(10,2),

    -- الإحالة
    referred_by INT,
    referral_code VARCHAR(20),

    -- الطلب الناتج
    order_id INT,

    -- الحالة
    status ENUM('active', 'cancelled', 'completed') DEFAULT 'active',
    cancelled_at DATETIME,
    cancellation_reason TEXT,

    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_deal (deal_id),
    INDEX idx_user (user_id),
    INDEX idx_phone (participant_phone),
    INDEX idx_status (status),

    FOREIGN KEY (deal_id) REFERENCES group_deals(id) ON DELETE RESTRICT,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
    FOREIGN KEY (referred_by) REFERENCES group_deal_participants(id) ON DELETE SET NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 4. مشاركات العروض
CREATE TABLE group_deal_shares (
    id INT AUTO_INCREMENT PRIMARY KEY,
    deal_id INT NOT NULL,
    shared_by INT NOT NULL,

    share_platform ENUM('whatsapp', 'facebook', 'twitter', 'telegram', 'copy_link', 'qr_code') NOT NULL,
    share_code VARCHAR(20) UNIQUE,

    clicks INT DEFAULT 0,
    conversions INT DEFAULT 0,

    shared_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_click_at DATETIME,

    INDEX idx_deal (deal_id),
    INDEX idx_shared_by (shared_by),
    INDEX idx_share_code (share_code),

    FOREIGN KEY (deal_id) REFERENCES group_deals(id) ON DELETE CASCADE,
    FOREIGN KEY (shared_by) REFERENCES group_deal_participants(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 5. إشعارات العروض
CREATE TABLE group_deal_notifications (
    id INT AUTO_INCREMENT PRIMARY KEY,
    deal_id INT NOT NULL,

    notification_type ENUM(
        'milestone_25', 'milestone_50', 'milestone_75', 'milestone_90',
        'price_drop', 'new_tier',
        'ending_soon_24h', 'ending_soon_1h',
        'success', 'failed', 'cancelled'
    ) NOT NULL,

    milestone_value INT,

    title VARCHAR(255) NOT NULL,
    title_ar VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    message_ar TEXT NOT NULL,

    sent_to_count INT DEFAULT 0,

    sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_deal (deal_id),
    INDEX idx_type (notification_type),

    FOREIGN KEY (deal_id) REFERENCES group_deals(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 6. سجل مشاهدات العروض
CREATE TABLE group_deal_views (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    deal_id INT NOT NULL,
    user_id INT,

    ip_address VARCHAR(45),
    user_agent TEXT,
    referrer VARCHAR(500),
    share_code VARCHAR(20),

    viewed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_deal (deal_id),
    INDEX idx_viewed_at (viewed_at),

    FOREIGN KEY (deal_id) REFERENCES group_deals(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 5. واجهات برمجة التطبيقات (API Specification)

### 5.1 Endpoints Overview

| Method | Endpoint | الوصف | Auth |
|--------|----------|-------|------|
| GET | /api/group-deals | قائمة العروض النشطة | لا |
| GET | /api/group-deals/:id | تفاصيل عرض | لا |
| POST | /api/group-deals | إنشاء عرض جديد | نعم (بائع) |
| PUT | /api/group-deals/:id | تحديث عرض | نعم (مالك) |
| DELETE | /api/group-deals/:id | حذف/إلغاء عرض | نعم (مالك) |
| POST | /api/group-deals/:id/join | الانضمام لعرض | لا |
| DELETE | /api/group-deals/:id/leave | مغادرة عرض | نعم |
| POST | /api/group-deals/:id/share | مشاركة عرض | نعم |
| GET | /api/group-deals/:id/participants | قائمة المشاركين | نعم (مالك) |
| GET | /api/group-deals/:id/stats | إحصائيات العرض | نعم (مالك) |

### 5.2 API Details

#### GET /api/group-deals
**وصف:** الحصول على قائمة العروض الجماعية النشطة

**Query Parameters:**
| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| page | number | 1 | رقم الصفحة |
| limit | number | 20 | عدد العناصر |
| category | string | - | تصفية حسب الفئة |
| sort | string | "popular" | popular, ending_soon, newest, price_low, price_high |
| status | string | "active" | active, ending_soon |

**Response:**
```json
{
  "success": true,
  "data": {
    "deals": [
      {
        "id": 1,
        "dealCode": "GD-2026-001",
        "title": "iPhone 15 Pro Max",
        "titleAr": "آيفون 15 برو ماكس",
        "product": {
          "id": 100,
          "name": "iPhone 15 Pro Max 256GB",
          "image": "https://cdn.haderos.com/products/iphone15.jpg"
        },
        "originalPrice": 50000,
        "currentPrice": 42000,
        "discount": 16,
        "minParticipants": 50,
        "currentParticipants": 40,
        "progress": 80,
        "currentTier": {
          "name": "Silver",
          "nameAr": "فضي"
        },
        "nextTier": {
          "name": "Gold",
          "nameAr": "ذهبي",
          "price": 40000,
          "participantsNeeded": 10
        },
        "endTime": "2026-01-10T23:59:59Z",
        "timeRemaining": {
          "days": 8,
          "hours": 14,
          "minutes": 30
        },
        "status": "active"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 45,
      "totalPages": 3
    }
  }
}
```

#### POST /api/group-deals/:id/join
**وصف:** الانضمام لعرض جماعي

**Request Body:**
```json
{
  "participantName": "أحمد محمد",
  "participantPhone": "+201012345678",
  "participantEmail": "ahmed@example.com",
  "quantity": 1,
  "referralCode": "ABC123",
  "paymentMethod": "card"
}
```

**Response (Success):**
```json
{
  "success": true,
  "data": {
    "participantId": 150,
    "dealCode": "GD-2026-001",
    "position": 41,
    "lockedPrice": 42000,
    "quantity": 1,
    "total": 42000,
    "paymentRequired": true,
    "paymentUrl": "https://pay.haderos.com/checkout/abc123",
    "shareCode": "P150X",
    "shareMessage": "انضم معي في العرض الجماعي...",
    "shareLinks": {
      "whatsapp": "https://wa.me/?text=...",
      "facebook": "https://facebook.com/sharer/...",
      "copy": "https://haderos.com/gd/GD-2026-001?ref=P150X"
    }
  }
}
```

**Response (Error):**
```json
{
  "success": false,
  "error": {
    "code": "DEAL_FULL",
    "message": "تم الوصول للحد الأقصى من المشاركين",
    "messageAr": "تم الوصول للحد الأقصى من المشاركين"
  }
}
```

---

## 6. الأمان والامتثال (Security & Compliance)

### 6.1 KAIA Integration

```typescript
async function validateDealWithKAIA(deal: GroupDeal): Promise<KAIAResult> {
  const validations = [
    // 1. التحقق من المنتج
    await kaia.validateProduct(deal.productId, {
      checkHalal: true,
      checkProhibited: true,
      checkInterest: true
    }),

    // 2. التحقق من التسعير
    await kaia.validatePricing({
      originalPrice: deal.originalPrice,
      discountedPrices: deal.tiers.map(t => t.price),
      maxDiscount: calculateMaxDiscount(deal)
    }),

    // 3. التحقق من الشروط
    await kaia.validateTerms({
      refundPolicy: true,
      transparentPricing: true,
      noHiddenFees: true
    })
  ];

  return {
    approved: validations.every(v => v.approved),
    issues: validations.flatMap(v => v.issues),
    recommendations: validations.flatMap(v => v.recommendations)
  };
}
```

### 6.2 Fraud Prevention

```typescript
const fraudChecks = {
  // منع التكرار
  duplicateParticipation: async (phone: string, dealId: number) => {
    const existing = await findParticipant(phone, dealId);
    return !existing;
  },

  // التحقق من الهاتف
  phoneValidation: async (phone: string) => {
    return await validateEgyptianPhone(phone);
  },

  // Rate limiting
  rateLimiting: {
    maxJoinsPerIP: 5,
    maxJoinsPerPhone: 3,
    timeWindow: '1h'
  },

  // Suspicious patterns
  suspiciousPatterns: async (request: JoinRequest) => {
    // Check for bot patterns
    // Check for velocity
    // Check for coordinated activity
  }
};
```

---

## 7. التكامل مع الأنظمة الأخرى (Integrations)

### 7.1 Payment Gateway
```typescript
// تكامل مع بوابات الدفع
const paymentIntegrations = {
  card: {
    provider: 'Paymob',
    iframe: true,
    supportedCards: ['visa', 'mastercard', 'meeza']
  },
  wallet: {
    providers: ['vodafone_cash', 'etisalat_cash', 'orange_money', 'fawry']
  },
  instapay: {
    qrPayment: true,
    instantTransfer: true
  }
};
```

### 7.2 Notification Services
```typescript
// تكامل مع خدمات الإشعارات
const notificationIntegrations = {
  push: {
    provider: 'Firebase Cloud Messaging',
    platforms: ['web', 'ios', 'android']
  },
  sms: {
    provider: 'Twilio / Vodafone Business',
    unicode: true
  },
  whatsapp: {
    provider: 'WhatsApp Business API',
    templates: true
  },
  email: {
    provider: 'SendGrid',
    templates: true
  }
};
```

### 7.3 Analytics
```typescript
// تتبع الأحداث
const analyticsEvents = {
  dealViewed: { dealId, source, userId },
  dealShared: { dealId, platform, userId },
  dealJoined: { dealId, userId, price, quantity },
  dealPaymentCompleted: { dealId, userId, amount },
  dealSuccess: { dealId, participants, revenue },
  dealFailed: { dealId, participants, reason }
};
```

---

## 8. خطة الاختبار (Testing Plan)

### 8.1 Unit Tests
```typescript
describe('GroupBuyingService', () => {
  describe('calculateCurrentPrice', () => {
    it('should return original price when no participants', () => { });
    it('should return tier price when threshold met', () => { });
    it('should calculate next tier correctly', () => { });
  });

  describe('joinDeal', () => {
    it('should add participant successfully', () => { });
    it('should reject when deal is full', () => { });
    it('should reject duplicate phone', () => { });
  });

  describe('handleDealClose', () => {
    it('should mark success when min reached', () => { });
    it('should refund all when failed', () => { });
    it('should create orders on success', () => { });
  });
});
```

### 8.2 Integration Tests
```typescript
describe('Group Buying E2E', () => {
  it('should complete full buying journey', async () => {
    // 1. Create deal
    // 2. Join with multiple participants
    // 3. Complete payments
    // 4. Wait for deal to close
    // 5. Verify orders created
    // 6. Verify notifications sent
  });

  it('should handle failed deal correctly', async () => {
    // 1. Create deal with high minimum
    // 2. Join with few participants
    // 3. Let deal expire
    // 4. Verify refunds processed
    // 5. Verify failure notifications
  });
});
```

### 8.3 Load Tests
```yaml
scenarios:
  - name: "Peak Load"
    vus: 1000
    duration: "5m"
    endpoints:
      - GET /api/group-deals
      - POST /api/group-deals/:id/join

  - name: "Stress Test"
    vus: 5000
    duration: "10m"
    rampUp: "2m"
```

---

## 9. الجدول الزمني (Timeline)

| الأسبوع | المهام |
|---------|--------|
| 1 | Database Schema + Basic API |
| 2 | Join Flow + Payment Integration |
| 3 | Tier System + Price Calculation |
| 4 | Notifications + WhatsApp |
| 5 | Frontend: Listing + Details |
| 6 | Frontend: Join + Checkout |
| 7 | Admin Panel + Analytics |
| 8 | Testing + Bug Fixes |
| 9 | UAT + Performance Tuning |
| 10 | Soft Launch + Monitoring |

---

## 10. المخاطر والتخفيف (Risks & Mitigation)

| المخاطر | الاحتمال | التأثير | التخفيف |
|---------|----------|---------|---------|
| فشل الدفع | متوسط | عالي | Multiple payment providers, retry logic |
| تأخر الإشعارات | منخفض | متوسط | Queue system, fallback channels |
| احتيال | متوسط | عالي | Fraud detection, phone verification |
| حمل زائد | منخفض | عالي | Auto-scaling, rate limiting |
| عدم اكتمال العروض | عالي | متوسط | Better UX, reminders, lower minimums |

---

## 11. معايير النجاح (Success Metrics)

| المقياس | الهدف (شهر 1) | الهدف (شهر 3) | الهدف (شهر 6) |
|---------|---------------|---------------|---------------|
| عروض نشطة | 50 | 200 | 500 |
| مشاركين يومياً | 100 | 500 | 2000 |
| معدل نجاح العروض | 40% | 60% | 75% |
| معدل المشاركة | 2x | 3x | 4x |
| إيرادات | 100K ج.م | 500K ج.م | 2M ج.م |

---

## 12. تحليل المخاطر (Risk Assessment)

| المخاطر | الاحتمال | التأثير | خطة التخفيف |
|---------|----------|---------|-------------|
| عدم وصول العرض للحد الأدنى | متوسط | عالي | إرسال تذكيرات، تمديد تلقائي 24 ساعة، اقتراح عروض بديلة |
| إلغاء مشاركين بعد الانضمام | متوسط | متوسط | فترة إلغاء محدودة (2 ساعة)، نظام سمعة للمشاركين |
| تلاعب التجار بالأسعار | منخفض | عالي | مراقبة تلقائية للأسعار، مقارنة مع السوق، عقوبات صارمة |
| عدم توفر المخزون عند النجاح | منخفض | عالي | حجز مسبق للمخزون، تحديث فوري للكميات |
| مشاكل في التوصيل الجماعي | متوسط | متوسط | تقسيم الشحنات، تتبع منفصل لكل مشارك |
| سوء استخدام النظام (spam) | متوسط | منخفض | حد أقصى للعروض يومياً، تحقق من الحسابات |

---

## 13. خطة الطوارئ (Contingency Plan)

### 13.1 ماذا لو لم يصل العرض للحد الأدنى؟
```
السيناريو: عرض على 100 قميص، وصل 80 مشارك فقط
الإجراءات:
1. تمديد تلقائي 24 ساعة مع إشعار "باقي 20 فقط!"
2. إرسال العرض لمستخدمين مهتمين بنفس الفئة
3. عرض على المشاركين الحاليين: إضافة صديق = خصم إضافي 5%
4. إذا فشل: رد المبالغ تلقائياً خلال 24 ساعة + كوبون تعويضي 10%
```

### 13.2 ماذا لو انسحب مشاركون بعد النجاح؟
```
السيناريو: العرض نجح بـ 100 مشارك، ثم انسحب 20
الإجراءات:
1. إذا بقي فوق الحد الأدنى: استمرار بنفس السعر
2. إذا أصبح تحت الحد: إشعار للباقين بالسعر الجديد + خيار الإلغاء
3. تسجيل المنسحبين في "قائمة الموثوقية المنخفضة"
```

### 13.3 ماذا لو نفد المخزون؟
```
السيناريو: العرض على 200 قطعة، المخزون انخفض لـ 150
الإجراءات:
1. إيقاف قبول مشاركين جدد تلقائياً
2. إشعار للتاجر لتحديث المخزون
3. إذا كان أقل من الحد الأدنى: عرض بديل أو إلغاء
```

---

## 14. مؤشرات أداء إضافية (Additional KPIs)

| المقياس | التعريف | الهدف | طريقة الحساب |
|---------|---------|-------|--------------|
| **تكلفة اكتساب عميل (CAC)** | تكلفة جلب مشارك جديد | < 25 ج.م | إجمالي تكاليف التسويق ÷ عملاء جدد |
| **قيمة العميل مدى الحياة (LTV)** | إجمالي إنفاق العميل | > 500 ج.م | متوسط قيمة الطلب × عدد الطلبات السنوية × سنوات الاحتفاظ |
| **معدل التحويل** | نسبة المشاهدين للمشاركين | > 15% | مشاركين ÷ مشاهدات العرض |
| **معدل الإحالة الفيروسية** | مشاركين جدد لكل مشارك | > 0.5 | مشاركين من إحالة ÷ إجمالي المشاركين |
| **صافي نقاط الترويج (NPS)** | رضا العملاء | > 50 | استطلاع (مروجين - منتقدين) |
| **معدل الاحتفاظ** | عودة المشاركين | > 40% | مشاركين عائدين ÷ إجمالي المشاركين السابقين |
| **LTV:CAC Ratio** | كفاءة الاستثمار | > 3:1 | LTV ÷ CAC |

---

**نهاية المستند**

**الموافقات:**

| الدور | الاسم | التوقيع | التاريخ |
|-------|-------|---------|---------|
| Product Owner | | | |
| Tech Lead | | | |
| KAIA Officer | | | |
| Security Officer | | | |
