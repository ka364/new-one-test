# Product Requirements Document (PRD)
# الميزات المتقدمة - AR/VR, Voice Commerce, Carbon Footprint

---

## معلومات المستند

| البند | القيمة |
|-------|--------|
| **رقم المستند** | PRD-HADEROS-ADV-006 |
| **الإصدار** | 1.0 |
| **تاريخ الإنشاء** | 2 يناير 2026 |
| **الحالة** | مسودة للمراجعة |

---

# الجزء الأول: الواقع المعزز والافتراضي (AR/VR)

## 1. نظرة عامة

### 1.1 ملخص تنفيذي
نظام الواقع المعزز (AR) والواقع الافتراضي (VR) يتيح للعملاء تجربة المنتجات افتراضياً قبل الشراء، مما يقلل معدلات الإرجاع ويزيد الثقة في الشراء عبر الإنترنت.

### 1.2 حالات الاستخدام في السوق المصري

```
👗 الملابس والأزياء
   ├── تجربة الملابس افتراضياً (Virtual Try-On)
   ├── رؤية كيف يبدو الزي عليك
   └── مقارنة المقاسات

🛋️ الأثاث والديكور
   ├── وضع الأثاث في غرفتك بالكاميرا
   ├── رؤية الألوان في مساحتك الفعلية
   └── قياس التناسب مع الغرفة

💄 مستحضرات التجميل
   ├── تجربة ألوان المكياج
   ├── تجربة تسريحات الشعر
   └── اختيار الألوان المناسبة للبشرة

👓 النظارات والإكسسوارات
   ├── تجربة النظارات على وجهك
   ├── تجربة الساعات على معصمك
   └── تجربة المجوهرات

🏠 العقارات
   ├── جولات افتراضية في الشقق
   ├── رؤية التصميم الداخلي
   └── استكشاف المشاريع العقارية
```

### 1.3 التقنيات المستخدمة

| التقنية | الاستخدام | المنصة |
|---------|----------|--------|
| **ARKit** | تجارب AR على iOS | iPhone/iPad |
| **ARCore** | تجارب AR على Android | Android |
| **WebXR** | تجارب AR/VR في المتصفح | الويب |
| **3D Models** | نماذج المنتجات | جميع المنصات |
| **Face Tracking** | تجربة المكياج والنظارات | الكاميرا الأمامية |
| **Body Tracking** | تجربة الملابس | الكاميرا الخلفية |

---

## 2. المتطلبات الوظيفية

### 2.1 تجربة الملابس الافتراضية (Virtual Try-On)

#### FR-AR-001: تجربة الملابس
**الوصف:** رؤية كيف تبدو الملابس على جسم العميل

```typescript
interface VirtualTryOnClothing {
  // أنواع الملابس المدعومة
  supportedTypes: [
    'tops',           // قمصان، تيشيرتات، بلوزات
    'bottoms',        // بناطيل، جينز، تنانير
    'dresses',        // فساتين
    'outerwear',      // جاكيتات، معاطف
    'accessories',    // إكسسوارات
  ];

  // متطلبات النموذج ثلاثي الأبعاد
  modelRequirements: {
    format: 'GLTF' | 'GLB';
    maxFileSize: '10MB';
    textureResolution: '2K';
    animations: boolean;          // للحركة الطبيعية
  };

  // تقنية التتبع
  tracking: {
    type: 'body_pose';
    landmarks: 33;                // نقاط الجسم
    realtime: true;
    minConfidence: 0.7;
  };

  // الإخراج
  output: {
    overlayOnCamera: true;
    screenshot: true;
    videoRecording: true;         // لمشاركة على السوشيال
    shareToSocial: true;
  };
}
```

**واجهة تجربة الملابس:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  تجربة افتراضية - قميص قطن أبيض                           [X]      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                               │ │
│  │                                                               │ │
│  │                       [كاميرا مباشرة]                         │ │
│  │                                                               │ │
│  │                    👤 ← القميص يظهر على جسمك                 │ │
│  │                                                               │ │
│  │                                                               │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  💡 قف أمام الكاميرا ووجه جسمك للأمام                              │
│                                                                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                   │
│  │[أبيض]  │ │[أزرق]  │ │[أسود]  │ │[رمادي] │  ← الألوان المتاحة  │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                   │
│                                                                     │
│  المقاس: [S] [M] [L] [XL] [XXL]                                    │
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐                        │
│  │  📸 التقط صورة   │  │  🛒 أضف للسلة    │                        │
│  └──────────────────┘  └──────────────────┘                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 وضع الأثاث في الغرفة (AR Furniture)

#### FR-AR-002: تجربة الأثاث
**الوصف:** وضع الأثاث في مساحتك الفعلية عبر الكاميرا

```typescript
interface ARFurniturePlacement {
  // الميزات
  features: {
    surfaceDetection: true;       // اكتشاف الأسطح
    roomMapping: true;            // رسم خريطة الغرفة
    lightEstimation: true;        // تقدير الإضاءة
    shadowRendering: true;        // ظلال واقعية
    scaleAccuracy: '95%';         // دقة الحجم
  };

  // التفاعل
  interaction: {
    rotate: true;                 // تدوير الأثاث
    move: true;                   // تحريك الموقع
    scale: true;                  // تكبير/تصغير
    changeColor: true;            // تغيير اللون
    multipleItems: true;          // وضع عدة قطع
  };

  // القياس
  measurement: {
    showDimensions: true;         // عرض الأبعاد
    fitCheck: true;               // التحقق من المساحة
    unitSystem: 'metric';
  };
}
```

**واجهة وضع الأثاث:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  ضع الأثاث في غرفتك                                      [X]       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                               │ │
│  │           [كاميرا مباشرة للغرفة]                              │ │
│  │                                                               │ │
│  │              ┌─────────────┐                                  │ │
│  │              │   [كنبة]    │  ← يمكنك تحريكها بإصبعك         │ │
│  │              │   180x90    │                                  │ │
│  │              └─────────────┘                                  │ │
│  │         ▪ ▪ ▪ ▪ ▪ ▪ ▪ ▪ ▪ ← الأرضية المكتشفة                 │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  [↻ تدوير] [↔ تحريك] [⊕ تكبير] [🎨 اللون]                        │
│                                                                     │
│  الأبعاد: 180 × 90 × 85 سم                                         │
│  ✅ مناسب للمساحة المتاحة                                          │
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐                        │
│  │  📸 التقط صورة   │  │  🛒 أضف للسلة    │                        │
│  └──────────────────┘  └──────────────────┘                        │
│                                                                     │
│  💡 حرك هاتفك ببطء لمسح الغرفة                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.3 تجربة المكياج والنظارات (Face AR)

#### FR-AR-003: تجربة على الوجه
**الوصف:** تجربة المكياج والنظارات والإكسسوارات على الوجه

```typescript
interface FaceARTryOn {
  // أنواع المنتجات
  productTypes: {
    makeup: {
      lipstick: true;             // أحمر شفاه
      eyeshadow: true;            // ظلال عيون
      blush: true;                // بلاشر
      foundation: true;           // كريم أساس
      mascara: true;              // ماسكارا
    };
    eyewear: {
      sunglasses: true;           // نظارات شمس
      prescription: true;         // نظارات طبية
    };
    accessories: {
      earrings: true;             // حلق
      necklaces: true;            // قلادات
      headwear: true;             // إكسسوارات شعر
    };
  };

  // تتبع الوجه
  faceTracking: {
    landmarks: 468;               // نقاط الوجه
    realtime: true;
    expressions: true;            // تعبيرات الوجه
    multiface: false;             // وجه واحد فقط
  };

  // جودة الإخراج
  rendering: {
    realistic: true;
    lightAdaptive: true;          // تكيف مع الإضاءة
    skinToneMatch: true;          // مطابقة لون البشرة
  };
}
```

---

## 3. مخطط قاعدة البيانات (AR/VR)

```sql
-- جدول نماذج AR/VR
CREATE TABLE ar_models (
  id VARCHAR(36) PRIMARY KEY,
  product_id VARCHAR(36) NOT NULL,
  model_type ENUM('clothing', 'furniture', 'makeup', 'eyewear', 'accessory') NOT NULL,

  -- ملفات النموذج
  model_url VARCHAR(255) NOT NULL,             -- GLTF/GLB file
  model_size_bytes INT NOT NULL,
  thumbnail_url VARCHAR(255),
  preview_video_url VARCHAR(255),

  -- الأبعاد
  dimensions JSON,                             -- {"width": 100, "height": 200, "depth": 50}
  scale_factor DECIMAL(5, 3) DEFAULT 1.0,

  -- الألوان والخيارات
  color_variants JSON,                         -- [{"name": "أبيض", "hex": "#FFFFFF", "model_url": "..."}]

  -- متطلبات التشغيل
  min_ar_sdk_version VARCHAR(20),
  supported_platforms JSON,                    -- ["ios", "android", "web"]

  -- الإحصائيات
  total_tries INT DEFAULT 0,
  conversion_rate DECIMAL(5, 2) DEFAULT 0,

  status ENUM('processing', 'active', 'inactive') DEFAULT 'processing',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
  INDEX idx_product (product_id),
  INDEX idx_type (model_type)
);

-- جدول جلسات AR
CREATE TABLE ar_sessions (
  id VARCHAR(36) PRIMARY KEY,
  user_id VARCHAR(36),
  product_id VARCHAR(36) NOT NULL,
  model_id VARCHAR(36) NOT NULL,

  session_type ENUM('try_on', 'placement', 'face_ar') NOT NULL,
  device_type VARCHAR(50),
  platform VARCHAR(20),

  -- التفاعل
  duration_seconds INT,
  screenshots_taken INT DEFAULT 0,
  shared_to_social BOOLEAN DEFAULT FALSE,

  -- النتيجة
  added_to_cart BOOLEAN DEFAULT FALSE,
  purchased BOOLEAN DEFAULT FALSE,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_user (user_id),
  INDEX idx_product (product_id),
  INDEX idx_date (created_at)
);
```

---

# الجزء الثاني: التجارة الصوتية (Voice Commerce)

## 4. نظرة عامة

### 4.1 ملخص تنفيذي
نظام التجارة الصوتية يتيح للعملاء التسوق والتفاعل مع المنصة باستخدام الأوامر الصوتية بالعربية المصرية والفصحى، مما يسهل التسوق لكبار السن وذوي الاحتياجات الخاصة.

### 4.2 لماذا مهم لمصر؟

```
👴 كبار السن
   ├── صعوبة القراءة والكتابة
   ├── عدم الراحة مع الشاشات الصغيرة
   └── يفضلون التحدث على الكتابة

🚗 السائقون والمشغولون
   ├── أيديهم مشغولة
   ├── التسوق أثناء القيادة (بأمان)
   └── إضافة للسلة بدون توقف

♿ ذوو الاحتياجات الخاصة
   ├── ضعف البصر
   ├── صعوبات حركية
   └── وصول أسهل للتسوق

📱 الجميع
   ├── أسرع من الكتابة
   ├── طبيعي ومريح
   └── تجربة حوارية
```

### 4.3 اللغات واللهجات المدعومة

| اللغة/اللهجة | الدعم | مثال |
|--------------|-------|------|
| العربية الفصحى | ✅ كامل | "أريد شراء قميص أبيض" |
| المصرية | ✅ كامل | "عايز أشتري قميص أبيض" |
| السعودية | 🔄 قريباً | "أبغى قميص أبيض" |
| الإنجليزية | ✅ كامل | "I want to buy a white shirt" |

---

## 5. المتطلبات الوظيفية

### 5.1 المساعد الصوتي

#### FR-VC-001: التعرف على الكلام
**الوصف:** تحويل الكلام لنص وفهم النية

```typescript
interface VoiceRecognition {
  // التعرف على الكلام
  speechToText: {
    languages: ['ar-EG', 'ar-SA', 'en-US'];
    dialects: ['egyptian', 'gulf', 'levantine'];
    realtime: true;
    noiseCancellation: true;
    punctuation: true;
  };

  // فهم النية (NLU)
  intentRecognition: {
    intents: [
      'search_product',           // "دور على قميص"
      'add_to_cart',              // "ضيف ده للسلة"
      'remove_from_cart',         // "شيل ده من السلة"
      'checkout',                 // "خلص الطلب"
      'track_order',              // "فين طلبي"
      'get_help',                 // "عايز مساعدة"
      'navigate',                 // "روح للمنتجات"
      'compare',                  // "قارن بين دول"
      'get_recommendations',      // "اقترح عليا"
    ];

    entities: [
      'product_type',             // قميص، بنطلون
      'color',                    // أبيض، أزرق
      'size',                     // كبير، صغير، XL
      'price_range',              // رخيص، غالي، أقل من 200
      'brand',                    // نايكي، أديداس
      'quantity',                 // اتنين، تلاتة
    ];
  };

  // الرد الصوتي (TTS)
  textToSpeech: {
    voice: 'female-egyptian';     // صوت مصري طبيعي
    speed: 'normal';
    emotion: true;                // تعبير صوتي
  };
}
```

**سيناريوهات الاستخدام:**
```
┌─────────────────────────────────────────────────────────────────────┐
│                    أمثلة على المحادثات الصوتية                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  🎯 السيناريو 1: البحث عن منتج                                      │
│  ─────────────────────────────────────────────────────────────────  │
│  👤 المستخدم: "عايز قميص قطن أبيض بأقل من 200 جنيه"                │
│  🤖 المساعد: "لقيت 15 قميص قطن أبيض. الأسعار من 120 لـ 180 جنيه.   │
│              عايز أعرضهم عليك؟"                                     │
│  👤 المستخدم: "أيوه"                                                │
│  🤖 المساعد: "أول واحد قميص قطن مصري، مقاس M، بـ 150 جنيه.         │
│              عايز تشوف صورته؟"                                      │
│                                                                     │
│  🎯 السيناريو 2: إضافة للسلة                                        │
│  ─────────────────────────────────────────────────────────────────  │
│  👤 المستخدم: "ضيف اتنين من ده للسلة"                               │
│  🤖 المساعد: "تمام، ضفت 2 قميص قطن أبيض مقاس M للسلة.              │
│              المجموع دلوقتي 300 جنيه. عايز تكمل الطلب؟"            │
│                                                                     │
│  🎯 السيناريو 3: تتبع الطلب                                         │
│  ─────────────────────────────────────────────────────────────────  │
│  👤 المستخدم: "فين طلبي؟"                                          │
│  🤖 المساعد: "طلبك رقم 12345 خرج للتوصيل النهارده الساعة 10.       │
│              المندوب اسمه أحمد وهيوصل خلال ساعة تقريباً.           │
│              عايز تتصل بيه؟"                                        │
│                                                                     │
│  🎯 السيناريو 4: المساعدة                                           │
│  ─────────────────────────────────────────────────────────────────  │
│  👤 المستخدم: "إزاي أرجع المنتج؟"                                  │
│  🤖 المساعد: "عشان ترجع منتج، ممكن تعمل كده من صفحة الطلبات،       │
│              أو لو عايز أساعدك دلوقتي، قولي رقم الطلب."            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 تفعيل الصوت

#### FR-VC-002: طرق التفعيل
**الوصف:** كيفية بدء التفاعل الصوتي

```typescript
interface VoiceActivation {
  // طرق التفعيل
  methods: {
    wakeWord: {
      enabled: true;
      phrases: ['يا هاديروس', 'هاديروس', 'Hey Haderos'];
      sensitivity: 'medium';
      alwaysListening: false;     // فقط داخل التطبيق
    };

    buttonPress: {
      enabled: true;
      holdToTalk: true;           // اضغط مع الاستمرار
      tapToToggle: true;          // اضغط لبدء/إيقاف
    };

    gestureActivation: {
      enabled: true;
      gesture: 'long_press_anywhere';
    };
  };

  // التغذية الراجعة
  feedback: {
    visualIndicator: true;        // موجة صوتية متحركة
    hapticFeedback: true;         // اهتزاز
    soundFeedback: true;          // صوت بدء/انتهاء
  };
}
```

**واجهة المساعد الصوتي:**
```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                                                                     │
│                                                                     │
│                         ╭─────────────╮                            │
│                         │             │                            │
│                         │  ~~~~🎤~~~~ │  ← أتكلم دلوقتي...         │
│                         │             │                            │
│                         ╰─────────────╯                            │
│                                                                     │
│                    "عايز قميص أبيض مقاس..."                        │
│                                                                     │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│                         [إلغاء]                                     │
│                                                                     │
│  💡 اضغط مع الاستمرار للتحدث، أو قل "يا هاديروس"                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 الردود الصوتية

#### FR-VC-003: Text-to-Speech
**الوصف:** تحويل ردود النظام لصوت طبيعي

```typescript
interface VoiceResponse {
  // إعدادات الصوت
  voice: {
    language: 'ar-EG';
    gender: 'female';
    name: 'Nour';                 // اسم الصوت
    style: 'friendly';           // ودود ومساعد
  };

  // تخصيص الرد
  customization: {
    speedControl: true;          // تحكم في السرعة
    volumeControl: true;
    pauseResume: true;
    repeat: true;                // "كرر تاني"
  };

  // الذكاء
  smartResponses: {
    contextAware: true;          // يفهم السياق
    personalized: true;          // مخصص للمستخدم
    adaptive: true;              // يتكيف مع المستخدم
  };
}
```

---

## 6. مخطط قاعدة البيانات (Voice Commerce)

```sql
-- جدول جلسات الصوت
CREATE TABLE voice_sessions (
  id VARCHAR(36) PRIMARY KEY,
  user_id VARCHAR(36),
  device_id VARCHAR(100),

  started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  ended_at TIMESTAMP,
  duration_seconds INT,

  -- اللغة
  language VARCHAR(10) DEFAULT 'ar-EG',
  dialect VARCHAR(20),

  -- الجهاز
  device_type VARCHAR(50),
  platform VARCHAR(20),

  -- النتائج
  successful_commands INT DEFAULT 0,
  failed_commands INT DEFAULT 0,
  purchases_made BOOLEAN DEFAULT FALSE,
  total_spent DECIMAL(10, 2) DEFAULT 0,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_user (user_id),
  INDEX idx_date (started_at)
);

-- جدول الأوامر الصوتية
CREATE TABLE voice_commands (
  id VARCHAR(36) PRIMARY KEY,
  session_id VARCHAR(36) NOT NULL,
  user_id VARCHAR(36),

  -- المدخل
  audio_url VARCHAR(255),
  transcription TEXT NOT NULL,
  confidence DECIMAL(3, 2),

  -- التحليل
  intent VARCHAR(50),
  entities JSON,
  sentiment VARCHAR(20),

  -- النتيجة
  response_text TEXT,
  response_audio_url VARCHAR(255),
  action_taken VARCHAR(100),
  success BOOLEAN DEFAULT TRUE,

  -- التوقيت
  processing_time_ms INT,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (session_id) REFERENCES voice_sessions(id) ON DELETE CASCADE,
  INDEX idx_session (session_id),
  INDEX idx_intent (intent)
);

-- جدول التدريب
CREATE TABLE voice_training_data (
  id VARCHAR(36) PRIMARY KEY,

  phrase TEXT NOT NULL,                        -- الجملة
  dialect VARCHAR(20),                         -- اللهجة
  intent VARCHAR(50) NOT NULL,                 -- النية
  entities JSON,                               -- الكيانات

  -- التحقق
  verified BOOLEAN DEFAULT FALSE,
  verified_by VARCHAR(36),

  -- المصدر
  source ENUM('manual', 'user_feedback', 'ai_generated') DEFAULT 'manual',

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

# الجزء الثالث: تتبع البصمة الكربونية (Carbon Footprint)

## 7. نظرة عامة

### 7.1 ملخص تنفيذي
نظام تتبع البصمة الكربونية يحسب ويعرض الأثر البيئي لكل عملية شراء وتوصيل، ويشجع العملاء على اتخاذ خيارات صديقة للبيئة.

### 7.2 لماذا مهم؟

```
🌍 الوعي البيئي المتزايد
   ├── الجيل الجديد يهتم بالبيئة
   ├── الشركات العالمية تتجه للاستدامة
   └── مصر تستضيف COP27

📊 التمايز التنافسي
   ├── ميزة فريدة في السوق المصري
   ├── جذب العملاء المهتمين بالبيئة
   └── تحسين صورة العلامة التجارية

💰 توفير التكاليف
   ├── تحسين كفاءة التوصيل
   ├── تقليل الهدر في التغليف
   └── حوافز للشحن المجمع
```

### 7.3 عناصر البصمة الكربونية

| العنصر | المقياس | مثال |
|--------|---------|------|
| **التغليف** | kg CO2 per package | كرتون عادي: 0.5kg |
| **النقل** | kg CO2 per km | سيارة: 0.2kg/km |
| **التخزين** | kg CO2 per day | مستودع مكيف: 0.1kg |
| **الإنتاج** | kg CO2 per product | قميص: 5-10kg |
| **الإرجاع** | مضاعفة النقل | × 2 |

---

## 8. المتطلبات الوظيفية

### 8.1 حساب البصمة الكربونية

#### FR-CF-001: حساب الأثر البيئي
**الوصف:** حساب البصمة الكربونية لكل طلب

```typescript
interface CarbonFootprintCalculator {
  // عوامل الحساب
  factors: {
    // التغليف
    packaging: {
      cardboard: 0.5;             // kg CO2 per kg
      plastic: 3.0;
      paper: 0.3;
      biodegradable: 0.1;
    };

    // وسائل النقل
    transport: {
      motorcycle: 0.05;           // kg CO2 per km
      car: 0.12;
      van: 0.18;
      truck: 0.25;
      bicycle: 0;
      electric_vehicle: 0.02;
    };

    // نوع التوصيل
    deliveryType: {
      express: 1.5;               // معامل
      standard: 1.0;
      eco: 0.7;                   // مجمع مع طلبات أخرى
      pickup: 0.3;                // استلام من نقطة
    };
  };

  // الحساب
  calculate(order: Order): CarbonFootprint {
    let totalCO2 = 0;

    // 1. التغليف
    const packagingCO2 = calculatePackagingCO2(order.packaging);

    // 2. النقل
    const transportCO2 = calculateTransportCO2(
      order.distance,
      order.vehicleType,
      order.deliveryType
    );

    // 3. المنتجات (إذا متوفر)
    const productCO2 = order.items.reduce((sum, item) => {
      return sum + (item.carbonFootprint || estimateProductCO2(item));
    }, 0);

    return {
      total: packagingCO2 + transportCO2 + productCO2,
      breakdown: {
        packaging: packagingCO2,
        transport: transportCO2,
        products: productCO2,
      },
      comparison: getComparison(totalCO2),
    };
  };
}

interface CarbonFootprint {
  total: number;                  // إجمالي kg CO2
  breakdown: {
    packaging: number;
    transport: number;
    products: number;
  };
  comparison: {
    treesNeeded: number;          // عدد الأشجار للتعويض
    carKmEquivalent: number;      // يعادل كم كيلومتر بالسيارة
    phoneCharges: number;         // يعادل كم شحنة موبايل
  };
}
```

### 8.2 عرض البصمة للعميل

#### FR-CF-002: واجهة البصمة الكربونية
**الوصف:** عرض الأثر البيئي للعميل

**في صفحة الدفع:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  اختر طريقة التوصيل                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ○ توصيل سريع (نفس اليوم)                                          │
│    25 ج.م | 🌿 1.2 kg CO2                                           │
│                                                                     │
│  ● توصيل عادي (2-3 أيام)                                   ✓ أفضل  │
│    15 ج.م | 🌿 0.8 kg CO2                                           │
│                                                                     │
│  ○ توصيل صديق للبيئة (3-5 أيام)               🌱 الأقل أثراً        │
│    10 ج.م | 🌿 0.3 kg CO2                                           │
│    ↳ يتم تجميع طلبك مع طلبات أخرى في نفس المنطقة                   │
│                                                                     │
│  ○ استلام من نقطة (خزانة ذكية)               🌱 صديق للبيئة        │
│    5 ج.م | 🌿 0.2 kg CO2                                            │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  📦 تغليف                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ ○ تغليف عادي (كرتون + بلاستيك)    | 🌿 0.5 kg CO2          │   │
│  │ ● تغليف صديق للبيئة (قابل للتحلل) | 🌿 0.1 kg CO2 ✓        │   │
│  │   + 3 ج.م إضافية                                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**ملخص البصمة:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  🌿 البصمة الكربونية لطلبك                                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│     ┌─────────────────────────────────────────────────┐            │
│     │                                                 │            │
│     │          0.9 kg CO2                            │            │
│     │          ═══════════                           │            │
│     │                                                 │            │
│     │  ┌─────────────────────────────────────────┐  │            │
│     │  │ التغليف      ████░░░░░░░░  0.1 kg (11%)  │  │            │
│     │  │ التوصيل      █████████░░░  0.8 kg (89%)  │  │            │
│     │  └─────────────────────────────────────────┘  │            │
│     │                                                 │            │
│     └─────────────────────────────────────────────────┘            │
│                                                                     │
│  💡 هذا يعادل:                                                     │
│     🚗 7 كيلومتر بالسيارة                                          │
│     📱 45 شحنة موبايل                                              │
│     🌳 0.05 شجرة لتعويضه                                           │
│                                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│                                                                     │
│  🎯 نصائح لتقليل بصمتك:                                             │
│     • اختر التوصيل المجمع لتوفير 40%                               │
│     • اجمع طلباتك في طلب واحد                                      │
│     • اختر نقاط الاستلام القريبة                                   │
│                                                                     │
│  [🌳 تعويض البصمة بـ 2 ج.م]                                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.3 برنامج التعويض الكربوني

#### FR-CF-003: Carbon Offset
**الوصف:** إمكانية تعويض البصمة الكربونية

```typescript
interface CarbonOffsetProgram {
  // خيارات التعويض
  options: {
    // زراعة أشجار
    treePlanting: {
      costPerKg: 2;               // جنيه لكل kg CO2
      partner: 'مؤسسة الأشجار المصرية';
      certificate: true;
    };

    // طاقة متجددة
    renewableEnergy: {
      costPerKg: 1.5;
      partner: 'مشروع الطاقة الشمسية';
      certificate: true;
    };

    // مشاريع بيئية
    environmentalProjects: {
      costPerKg: 1;
      projects: ['تنظيف النيل', 'حماية الشعاب المرجانية'];
    };
  };

  // المكافآت
  rewards: {
    offsetPoints: {
      perKgOffset: 10;            // نقاط لكل kg معوض
      usableAsDiscount: true;
    };

    badges: [
      { name: 'صديق البيئة', requirement: '10 kg offset' },
      { name: 'بطل المناخ', requirement: '100 kg offset' },
      { name: 'محارب الكربون', requirement: '500 kg offset' },
    ];

    leaderboard: true;            // ترتيب أكثر المعوضين
  };
}
```

### 8.4 لوحة تحكم الاستدامة

#### FR-CF-004: Sustainability Dashboard
**الوصف:** لوحة تحكم للعميل لتتبع أثره البيئي

```
┌─────────────────────────────────────────────────────────────────────┐
│  🌍 لوحة الاستدامة الخاصة بك                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  📊 إحصائياتك هذا الشهر                                             │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐         │
│  │ البصمة     │ تم تعويضه   │ توفير       │ ترتيبك      │         │
│  │ الإجمالية  │             │             │             │         │
│  │  12.5 kg   │   8.0 kg    │   -35%      │  #234       │         │
│  │  ↓ 20%     │   ↑ 50%     │  من المعدل  │  من 10,000  │         │
│  └─────────────┴─────────────┴─────────────┴─────────────┘         │
│                                                                     │
│  📈 تطور بصمتك الكربونية                                            │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │  15│    ___                                             │       │
│  │  12│___/   \___                                         │       │
│  │   9│           \___                                     │       │
│  │   6│               \___/\___                            │       │
│  │   3│                        \___                        │       │
│  │   0└──────────────────────────────────────────────────→ │       │
│  │     يناير  فبراير  مارس  أبريل  مايو  يونيو            │       │
│  └─────────────────────────────────────────────────────────┘       │
│                                                                     │
│  🏆 شاراتك                                                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                   │
│  │  🌱     │ │  🌿     │ │  ⬜    │ │  ⬜    │                   │
│  │ صديق   │ │ مُعوِّض  │ │ بطل    │ │ محارب  │                   │
│  │ البيئة │ │ الكربون │ │ المناخ │ │ الكربون│                   │
│  │  ✓     │ │   ✓     │ │  75%   │ │  20%   │                   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                   │
│                                                                     │
│  🌳 مساهماتك في التعويض                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  إجمالي التعويض: 8.0 kg CO2                                 │   │
│  │  المبلغ المتبرع به: 16 ج.م                                  │   │
│  │  الأشجار المزروعة باسمك: 2 🌳                               │   │
│  │                                                             │   │
│  │  [📜 عرض شهادات التعويض]                                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 9. مخطط قاعدة البيانات (Carbon Footprint)

```sql
-- جدول البصمة الكربونية للطلبات
CREATE TABLE order_carbon_footprint (
  id VARCHAR(36) PRIMARY KEY,
  order_id VARCHAR(36) NOT NULL UNIQUE,

  -- البصمة
  total_co2_kg DECIMAL(10, 3) NOT NULL,
  packaging_co2_kg DECIMAL(10, 3) DEFAULT 0,
  transport_co2_kg DECIMAL(10, 3) DEFAULT 0,
  product_co2_kg DECIMAL(10, 3) DEFAULT 0,

  -- تفاصيل الحساب
  calculation_details JSON,

  -- التعويض
  offset_amount_kg DECIMAL(10, 3) DEFAULT 0,
  offset_cost_egp DECIMAL(10, 2) DEFAULT 0,
  offset_project VARCHAR(100),
  offset_certificate_url VARCHAR(255),

  -- المقارنات
  vs_average_percent DECIMAL(5, 2),           -- مقارنة بالمتوسط

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE
);

-- جدول إحصائيات المستخدم
CREATE TABLE user_carbon_stats (
  id VARCHAR(36) PRIMARY KEY,
  user_id VARCHAR(36) NOT NULL UNIQUE,

  -- الإحصائيات الإجمالية
  total_orders INT DEFAULT 0,
  total_co2_kg DECIMAL(10, 3) DEFAULT 0,
  total_offset_kg DECIMAL(10, 3) DEFAULT 0,
  total_offset_spent_egp DECIMAL(10, 2) DEFAULT 0,

  -- الإحصائيات الشهرية (آخر 12 شهر)
  monthly_stats JSON,

  -- الترتيب
  rank_position INT,
  rank_updated_at TIMESTAMP,

  -- الشارات
  badges JSON,                                 -- ["eco_friend", "carbon_fighter"]

  -- خيارات التسليم المفضلة
  preferred_delivery_type VARCHAR(20),
  preferred_packaging_type VARCHAR(20),

  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

-- جدول عوامل البصمة الكربونية
CREATE TABLE carbon_factors (
  id VARCHAR(36) PRIMARY KEY,
  category VARCHAR(50) NOT NULL,               -- packaging, transport, product
  subcategory VARCHAR(50) NOT NULL,            -- cardboard, motorcycle, tshirt
  factor_value DECIMAL(10, 5) NOT NULL,        -- kg CO2 per unit
  unit VARCHAR(20) NOT NULL,                   -- kg, km, piece

  description VARCHAR(255),
  source VARCHAR(255),                         -- مصدر البيانات
  valid_from DATE,
  valid_to DATE,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  UNIQUE KEY uk_category (category, subcategory)
);

-- جدول مشاريع التعويض
CREATE TABLE carbon_offset_projects (
  id VARCHAR(36) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  name_ar VARCHAR(100) NOT NULL,
  type ENUM('tree_planting', 'renewable_energy', 'conservation') NOT NULL,

  description TEXT,
  partner_name VARCHAR(100),
  location VARCHAR(100),

  cost_per_kg_co2 DECIMAL(10, 2) NOT NULL,
  total_offset_available_kg DECIMAL(15, 3),
  total_offset_used_kg DECIMAL(15, 3) DEFAULT 0,

  certificate_template_url VARCHAR(255),
  images JSON,

  status ENUM('active', 'completed', 'paused') DEFAULT 'active',

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 10. واجهات برمجة التطبيقات المشتركة

```typescript
// ==================== AR/VR APIs ====================

/**
 * @api {get} /api/ar/models/:productId الحصول على نموذج AR
 */
GET /api/ar/models/:productId

/**
 * @api {post} /api/ar/sessions تسجيل جلسة AR
 */
POST /api/ar/sessions


// ==================== Voice Commerce APIs ====================

/**
 * @api {post} /api/voice/command إرسال أمر صوتي
 * @apiBody {string} audio - ملف الصوت (base64)
 * @apiBody {string} language - اللغة
 */
POST /api/voice/command

/**
 * @api {get} /api/voice/history سجل الأوامر الصوتية
 */
GET /api/voice/history


// ==================== Carbon Footprint APIs ====================

/**
 * @api {get} /api/carbon/calculate/:orderId حساب البصمة الكربونية
 */
GET /api/carbon/calculate/:orderId

/**
 * @api {post} /api/carbon/offset تعويض البصمة
 * @apiBody {string} orderId - رقم الطلب
 * @apiBody {string} projectId - مشروع التعويض
 */
POST /api/carbon/offset

/**
 * @api {get} /api/carbon/stats إحصائيات المستخدم
 */
GET /api/carbon/stats

/**
 * @api {get} /api/carbon/leaderboard ترتيب المعوضين
 */
GET /api/carbon/leaderboard
```

---

## 11. التكامل مع KAIA

```typescript
interface AdvancedFeaturesKAIACompliance {
  // AR/VR
  arVr: {
    // لا صور غير لائقة
    contentModeration: true;
    // الخصوصية في تجارب الوجه
    faceDataProtection: {
      noStorage: true;
      localProcessingOnly: true;
    };
  };

  // Voice Commerce
  voice: {
    // حماية الخصوصية
    voiceDataRetention: '24 hours';
    noVoiceProfiling: true;
    // الشفافية
    disclosureOfAI: true;
  };

  // Carbon Footprint
  carbon: {
    // دقة الحسابات
    verifiedFactors: true;
    auditableCalculations: true;
    // شفافية التعويض
    certifiedPartners: true;
    publicReporting: true;
  };
}
```

---

## 12. مقاييس النجاح المشتركة

### AR/VR KPIs
| المقياس | الهدف | طريقة القياس |
|---------|-------|-------------|
| معدل استخدام AR | 20% من صفحات المنتجات | جلسات AR / زيارات |
| تحسن معدل التحويل | +30% | مبيعات مع AR vs بدون |
| تقليل الإرجاع | -25% | إرجاعات منتجات AR |

### Voice Commerce KPIs
| المقياس | الهدف | طريقة القياس |
|---------|-------|-------------|
| دقة التعرف | > 95% | الأوامر المفهومة / الإجمالي |
| معدل إتمام المهام | > 80% | المهام المكتملة بنجاح |
| رضا المستخدمين | > 4.0/5 | استطلاعات |

### Carbon Footprint KPIs
| المقياس | الهدف | طريقة القياس |
|---------|-------|-------------|
| نسبة اختيار الخيار الأخضر | > 30% | الطلبات الصديقة للبيئة |
| معدل التعويض | > 10% | الطلبات مع تعويض |
| تقليل البصمة المتوسطة | -15% | بعد 6 أشهر |

---

## 13. تقدير التكاليف الإجمالية

| الميزة | تكلفة التطوير | التشغيل الشهري |
|--------|--------------|----------------|
| **AR/VR** | | |
| - التطوير الأساسي | 350,000 ج.م | |
| - إنشاء نماذج 3D (100 منتج) | 100,000 ج.م | |
| - البنية التحتية | | 15,000 ج.م |
| **Voice Commerce** | | |
| - تطوير NLU العربي | 400,000 ج.م | |
| - تدريب النموذج | 100,000 ج.م | |
| - سيرفرات معالجة | | 25,000 ج.م |
| **Carbon Footprint** | | |
| - تطوير النظام | 150,000 ج.م | |
| - التكاملات | 50,000 ج.م | |
| - إدارة البرنامج | | 10,000 ج.م |
| | | |
| **الإجمالي** | **1,150,000 ج.م** | **50,000 ج.م/شهر** |

---

## 14. خطة التنفيذ

### الجدول الزمني
```
Q1 2026: AR للأثاث + Voice (MVP)
   - تجربة وضع الأثاث بالكاميرا
   - المساعد الصوتي الأساسي (بحث + سلة)

Q2 2026: AR للملابس + Voice المتقدم
   - تجربة الملابس الافتراضية
   - الأوامر الصوتية المتقدمة (طلبات، تتبع)

Q3 2026: Face AR + Carbon Footprint
   - تجربة المكياج والنظارات
   - حساب وعرض البصمة الكربونية
   - برنامج التعويض

Q4 2026: التحسين والتوسع
   - تحسين دقة AR
   - توسيع Voice للهجات
   - شراكات بيئية جديدة
```

---

## 15. تحليل المخاطر الشامل (Risk Assessment)

### 15.1 مخاطر AR/VR

| المخاطر | الاحتمال | التأثير | خطة التخفيف |
|---------|----------|---------|-------------|
| أجهزة لا تدعم AR | عالي | متوسط | fallback لصور 360°، قائمة أجهزة مدعومة |
| نماذج 3D غير دقيقة | متوسط | عالي | معايير جودة صارمة، اختبار على أجهزة متعددة |
| تجربة سيئة تزيد الإرجاع | منخفض | عالي | تحسين مستمر، مقارنة معدلات الإرجاع |
| مشاكل الخصوصية (كاميرا) | منخفض | عالي | معالجة محلية، لا تخزين للصور، شفافية |

### 15.2 مخاطر Voice Commerce

| المخاطر | الاحتمال | التأثير | خطة التخفيف |
|---------|----------|---------|-------------|
| عدم فهم اللهجة المصرية | متوسط | عالي | تدريب مستمر، feedback من المستخدمين |
| أوامر خاطئة (شراء غير مقصود) | منخفض | عالي | تأكيد قبل الشراء، إلغاء سهل |
| مشاكل الخصوصية (تسجيل الصوت) | متوسط | عالي | لا تخزين دائم، شفافية، حذف عند الطلب |
| بطء الاستجابة | متوسط | متوسط | تحسين السيرفرات، معالجة محلية جزئية |

### 15.3 مخاطر Carbon Footprint

| المخاطر | الاحتمال | التأثير | خطة التخفيف |
|---------|----------|---------|-------------|
| حسابات غير دقيقة | متوسط | متوسط | مصادر موثوقة، تحديث دوري، شفافية |
| عدم اهتمام المستخدمين | عالي | منخفض | gamification، حوافز، تبسيط |
| شراكات تعويض غير موثوقة | منخفض | عالي | فحص دقيق للشركاء، شهادات معتمدة |

---

## 16. خطة الطوارئ الشاملة (Contingency Plans)

### 16.1 AR/VR

**ماذا لو فشلت تجربة AR على جهاز العميل؟**
```
الإجراءات:
1. fallback تلقائي لصور عادية + فيديو 360°
2. رسالة واضحة "جهازك لا يدعم هذه الميزة"
3. اقتراح زيارة أقرب معرض للتجربة الفعلية
4. عرض سياسة إرجاع مرنة
```

### 16.2 Voice Commerce

**ماذا لو نفذ المساعد أمراً خاطئاً؟**
```
الإجراءات:
1. تأكيد صوتي قبل أي عملية شراء
2. إشعار فوري بالعملية
3. زر "تراجع" بارز في التطبيق
4. إمكانية الإلغاء خلال 30 دقيقة بدون رسوم
5. تسجيل الحادثة لتحسين النموذج
```

### 16.3 Carbon Footprint

**ماذا لو تبين أن شريك التعويض غير موثوق؟**
```
الإجراءات:
1. إيقاف الشراكة فوراً
2. إشعار جميع المتبرعين
3. تحويل التبرعات لشريك بديل موثوق
4. تعويض المتضررين بمضاعفة تعويضهم
5. نشر تقرير شفافية
```

---

## 17. مؤشرات أداء إضافية شاملة (Additional KPIs)

### AR/VR KPIs

| المقياس | التعريف | الهدف |
|---------|---------|-------|
| **معدل التبني** | مستخدمون جربوا AR | > 30% من الزوار |
| **تحسن التحويل** | فرق التحويل مع/بدون AR | > +25% |
| **تقليل الإرجاع** | فرق الإرجاع مع/بدون AR | > -20% |
| **NPS للميزة** | رضا مستخدمي AR | > 50 |

### Voice Commerce KPIs

| المقياس | التعريف | الهدف |
|---------|---------|-------|
| **معدل النجاح** | أوامر مفهومة ومنفذة | > 90% |
| **NPS الصوتي** | رضا المستخدمين | > 45 |
| **معدل التبني المتكرر** | عودة لاستخدام الصوت | > 40% |
| **وقت إتمام المهمة** | صوت vs كتابة | أسرع 30% |

### Carbon Footprint KPIs

| المقياس | التعريف | الهدف |
|---------|---------|-------|
| **معدل التعويض** | طلبات مع تعويض | > 15% |
| **إجمالي CO2 معوض** | بالطن سنوياً | > 100 طن |
| **معدل اختيار الأخضر** | توصيل صديق للبيئة | > 25% |
| **تحسن البصمة** | تقليل متوسط CO2 | > -20% |

---

**نهاية المستند**
