# المرجع الفني الشامل - HADEROS AI Cloud
## Technical Reference for Development Team

**الإصدار:** 1.0.0
**آخر تحديث:** 1 يناير 2026
**الحالة:** جاهز للإنتاج ✅

---

## جدول المحتويات

1. [نظرة عامة على النظام](#نظرة-عامة-على-النظام)
2. [البنية التقنية](#البنية-التقنية)
3. [خدمات المستخدمين](#خدمات-المستخدمين)
4. [الوحدات الأساسية](#الوحدات-الأساسية)
5. [Bio-Modules](#bio-modules)
6. [التكاملات الخارجية](#التكاملات-الخارجية)
7. [دليل التطوير](#دليل-التطوير)
8. [دليل التشغيل](#دليل-التشغيل)
9. [الأمان](#الأمان)
10. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

# 1. نظرة عامة على النظام

## ما هو HADEROS؟

**HADEROS AI Cloud** هو نظام تشغيل متكامل للتجارة الإلكترونية مصمم خصيصاً للسوق المصري والعربي. يجمع بين:

- **إدارة الطلبات** (Orders Management)
- **إدارة المخزون** (Inventory Management)
- **التكامل مع الشحن** (Shipping Integration)
- **بوابات الدفع المصرية** (Egyptian Payment Gateways)
- **التجارة عبر واتساب** (WhatsApp Commerce)
- **نظام التقسيط** (BNPL - Buy Now Pay Later)
- **الذكاء الاصطناعي** (AI-Powered Insights)

## الإحصائيات الفنية

```
┌─────────────────────────────────────────────────────────────┐
│                    HADEROS في أرقام                        │
├─────────────────────────────────────────────────────────────┤
│  📊 Database Schemas      │  32 ملف                        │
│  🔌 API Routers           │  70+ ملف                       │
│  🧬 Bio-Modules           │  54 ملف                        │
│  ⚙️  Services              │  27 ملف                        │
│  🔗 Integrations          │  18 ملف                        │
│  📚 Documentation         │  234+ ملف                      │
│  💾 Total Lines of Code   │  ~900,000 سطر                  │
└─────────────────────────────────────────────────────────────┘
```

---

# 2. البنية التقنية

## Technology Stack

```
┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND                               │
├─────────────────────────────────────────────────────────────┤
│  React 18          │  واجهة المستخدم الرئيسية              │
│  TypeScript 5      │  Type Safety                          │
│  Vite 7            │  Build Tool                           │
│  TailwindCSS       │  Styling                              │
│  Shadcn/UI         │  Component Library                    │
│  React Query       │  Data Fetching                        │
│  Zustand           │  State Management                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      BACKEND                                │
├─────────────────────────────────────────────────────────────┤
│  Node.js 20+       │  Runtime                              │
│  Express           │  HTTP Server                          │
│  tRPC              │  Type-safe API                        │
│  Drizzle ORM       │  Database ORM                         │
│  PostgreSQL        │  Database                             │
│  Zod               │  Validation                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    INFRASTRUCTURE                           │
├─────────────────────────────────────────────────────────────┤
│  Docker            │  Containerization                     │
│  PM2               │  Process Management                   │
│  Nginx             │  Reverse Proxy                        │
│  Redis (optional)  │  Caching                              │
└─────────────────────────────────────────────────────────────┘
```

## هيكل المشروع

```
HADEROS-AI-CLOUD/
├── apps/
│   └── haderos-web/                 # التطبيق الرئيسي
│       ├── drizzle/                 # Database Schemas
│       │   ├── schema.ts            # Schema الرئيسي
│       │   ├── schema-payments.ts   # نظام الدفع
│       │   ├── schema-bnpl.ts       # نظام التقسيط
│       │   ├── schema-whatsapp.ts   # واتساب
│       │   ├── schema-shipping.ts   # الشحن
│       │   └── ... (32 ملف)
│       │
│       ├── server/                  # Backend
│       │   ├── _core/               # Core utilities
│       │   │   ├── trpc.ts          # tRPC setup
│       │   │   ├── env.ts           # Environment
│       │   │   └── llm.ts           # AI integration
│       │   │
│       │   ├── routers/             # API Endpoints
│       │   │   ├── orders.ts        # طلبات
│       │   │   ├── products.ts      # منتجات
│       │   │   ├── payment.ts       # دفع
│       │   │   ├── bnpl.ts          # تقسيط
│       │   │   ├── shopify.ts       # Shopify
│       │   │   ├── shipments.ts     # شحن
│       │   │   └── ... (70+ ملف)
│       │   │
│       │   ├── services/            # Business Logic
│       │   │   ├── unified-payment.service.ts
│       │   │   ├── shopify-webhook.service.ts
│       │   │   └── ... (27 ملف)
│       │   │
│       │   ├── integrations/        # External APIs
│       │   │   ├── shopify-api.ts
│       │   │   ├── bosta-api.ts
│       │   │   ├── instapay.ts
│       │   │   └── ... (18 ملف)
│       │   │
│       │   └── bio-modules/         # AI Modules
│       │       ├── tardigrade.ts    # المرونة
│       │       ├── chameleon.ts     # التكيف
│       │       ├── cephalopod.ts    # التعلم
│       │       └── ... (54 ملف)
│       │
│       ├── pages/                   # Frontend Pages
│       ├── components/              # UI Components
│       └── hooks/                   # React Hooks
│
├── docs/                            # الوثائق
├── scripts/                         # سكريبتات
└── packages/                        # Shared packages
```

## Database Schema Overview

```sql
-- الجداول الرئيسية
┌─────────────────────────────────────────────────────────────┐
│ CORE TABLES                                                 │
├─────────────────────────────────────────────────────────────┤
│ users              │ المستخدمين والموظفين                   │
│ orders             │ الطلبات                               │
│ products           │ المنتجات                              │
│ customers          │ العملاء                               │
│ inventory          │ المخزون                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ PAYMENT TABLES                                              │
├─────────────────────────────────────────────────────────────┤
│ payment_transactions    │ المعاملات المالية                 │
│ payment_providers       │ بوابات الدفع                      │
│ payment_refunds         │ المرتجعات                         │
│ mobile_wallet_payments  │ المحافظ الإلكترونية               │
│ fawry_references        │ مراجع فوري                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ SHIPPING TABLES                                             │
├─────────────────────────────────────────────────────────────┤
│ shipments              │ الشحنات                            │
│ shipping_providers     │ شركات الشحن                        │
│ delivery_zones         │ مناطق التوصيل                      │
│ cod_collections        │ تحصيل COD                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ BNPL TABLES                                                 │
├─────────────────────────────────────────────────────────────┤
│ bnpl_providers         │ مقدمي التقسيط                      │
│ bnpl_applications      │ طلبات التقسيط                      │
│ bnpl_installments      │ الأقساط                            │
│ bnpl_transactions      │ معاملات التقسيط                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ WHATSAPP TABLES                                             │
├─────────────────────────────────────────────────────────────┤
│ whatsapp_catalogs      │ كتالوجات واتساب                    │
│ whatsapp_carts         │ سلات التسوق                        │
│ whatsapp_conversations │ المحادثات                          │
│ whatsapp_templates     │ القوالب                            │
└─────────────────────────────────────────────────────────────┘
```

---

# 3. خدمات المستخدمين

## نظرة عامة على أنواع المستخدمين

```
┌─────────────────────────────────────────────────────────────┐
│                    أنواع المستخدمين                         │
├─────────────────────────────────────────────────────────────┤
│  🏭 المُصنّع (Manufacturer)                                 │
│  🏪 التاجر (Merchant)                                       │
│  🚚 شركة الشحن (Shipping Company)                           │
│  👔 الموظف (Employee)                                       │
│  📣 المسوّق (Marketer)                                      │
│  👤 العميل النهائي (End Customer)                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏭 خدمات المُصنّع (Manufacturer)

### الوصف
المُصنّع هو من ينتج المنتجات ويريد بيعها للتجار أو مباشرة للعملاء.

### الخدمات المتاحة

| الخدمة | الوصف | الـ Router |
|--------|-------|------------|
| **إدارة المنتجات** | إضافة، تعديل، حذف المنتجات | `products.ts` |
| **إدارة المخزون** | تتبع الكميات والتنبيهات | `inventory.ts` |
| **التسعير بالجملة** | أسعار خاصة للتجار | `products.ts` |
| **تتبع الطلبات** | متابعة طلبات التجار | `orders.ts` |
| **تقارير الإنتاج** | تحليلات المبيعات | `bio-dashboard.ts` |
| **إدارة الموزعين** | ربط مع التجار | `founders.ts` |

### API Endpoints

```typescript
// إضافة منتج جديد
POST /api/trpc/products.create
{
  name: "حذاء رياضي",
  sku: "SHOE-001",
  price: 299.99,
  wholesalePrice: 199.99,  // سعر الجملة
  inventory: 500,
  category: "أحذية"
}

// تحديث المخزون
POST /api/trpc/inventory.update
{
  productId: 1,
  quantity: 100,
  type: "add" // أو "subtract"
}

// تقرير المبيعات
GET /api/trpc/bio.getSalesReport?input={"period":"monthly"}
```

### الواجهات

```
/dashboard           - لوحة التحكم الرئيسية
/products            - إدارة المنتجات
/inventory           - إدارة المخزون
/orders/wholesale    - طلبات الجملة
/reports/production  - تقارير الإنتاج
```

---

## 🏪 خدمات التاجر (Merchant)

### الوصف
التاجر يشتري من المُصنّع أو المورد ويبيع للعملاء النهائيين.

### الخدمات المتاحة

| الخدمة | الوصف | الـ Router |
|--------|-------|------------|
| **إدارة المتجر** | إعداد المتجر وتخصيصه | `shopify.ts` |
| **استقبال الطلبات** | من جميع القنوات | `orders.ts` |
| **التجارة عبر واتساب** | بيع عبر واتساب | `whatsapp-commerce.ts` |
| **المبيعات الهاتفية** | نظام Phone Sales | `orders.ts` |
| **إدارة الشحن** | ربط مع شركات الشحن | `shipments.ts` |
| **بوابات الدفع** | كل طرق الدفع المصرية | `payment.ts` |
| **نظام التقسيط** | BNPL للعملاء | `bnpl.ts` |
| **CRM** | إدارة علاقات العملاء | `messaging.ts` |
| **التقارير** | تحليلات شاملة | `bio-dashboard.ts` |

### API Endpoints

```typescript
// مزامنة من Shopify
POST /api/trpc/shopify.syncProducts

// إنشاء طلب جديد
POST /api/trpc/orders.create
{
  customerName: "أحمد محمد",
  customerPhone: "01012345678",
  items: [{productId: 1, quantity: 2, price: 299.99}],
  shippingAddress: "القاهرة، مدينة نصر",
  paymentMethod: "cod"
}

// إنشاء شحنة
POST /api/trpc/shipments.create
{
  orderId: 123,
  provider: "bosta",
  recipientName: "أحمد محمد",
  recipientPhone: "01012345678",
  address: "15 شارع التحرير",
  governorate: "القاهرة"
}

// إنشاء عقد تقسيط
POST /api/trpc/bnpl.createContract
{
  orderId: 123,
  customerId: 456,
  totalAmount: 1500,
  installments: 6
}
```

### التكاملات المتاحة للتاجر

```
┌─────────────────────────────────────────────────────────────┐
│                    تكاملات التاجر                           │
├─────────────────────────────────────────────────────────────┤
│  🛒 Shopify          │ مزامنة المنتجات والطلبات             │
│  📦 Bosta            │ شحن داخل مصر                        │
│  📦 J&T Express      │ شحن سريع                            │
│  💳 InstaPay         │ تحويل فوري                          │
│  💳 PayMob           │ بطاقات ائتمان                       │
│  💳 Fawry            │ دفع بالرقم المرجعي                  │
│  📱 فودافون كاش      │ محفظة إلكترونية                     │
│  📱 اتصالات كاش      │ محفظة إلكترونية                     │
│  💬 WhatsApp Business│ تجارة عبر واتساب                    │
│  💰 ValU             │ تقسيط                               │
│  💰 Sympl            │ تقسيط                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚚 خدمات شركة الشحن (Shipping Company)

### الوصف
شركة الشحن تستلم الطلبات من التجار وتوصلها للعملاء.

### الخدمات المتاحة

| الخدمة | الوصف | الـ Router |
|--------|-------|------------|
| **استلام الشحنات** | من التجار | `shipments.ts` |
| **تتبع الشحنات** | Real-time tracking | `shipments.ts` |
| **تحصيل COD** | الدفع عند الاستلام | `cod.router.ts` |
| **إدارة المناديب** | توزيع الشحنات | `employees.ts` |
| **تقارير التوصيل** | أداء التوصيل | `metrics.ts` |
| **Webhooks** | إشعارات للتجار | `webhooks.ts` |

### API Endpoints

```typescript
// تحديث حالة الشحنة
POST /api/trpc/shipments.updateStatus
{
  trackingNumber: "BST-123456",
  status: "delivered",
  deliveredAt: "2024-01-15T14:30:00Z"
}

// تسجيل تحصيل COD
POST /api/trpc/cod.recordCollection
{
  shipmentId: 123,
  collectedAmount: 599.99,
  collectedBy: "employee_id"
}

// تتبع شحنة
GET /api/trpc/shipments.track?input={"trackingNumber":"BST-123456"}
```

### Webhook Events

```typescript
// عند تغيير حالة الشحنة
POST /api/webhooks/shipping
{
  event: "shipment.status_changed",
  data: {
    trackingNumber: "BST-123456",
    oldStatus: "in_transit",
    newStatus: "delivered",
    timestamp: "2024-01-15T14:30:00Z"
  }
}
```

---

## 👔 خدمات الموظف (Employee)

### الوصف
الموظف يعمل لدى التاجر في المبيعات أو خدمة العملاء أو الشحن.

### أنواع الموظفين

| النوع | الصلاحيات | الـ Router |
|-------|----------|------------|
| **مندوب مبيعات** | إنشاء طلبات، اتصالات | `orders.ts` |
| **خدمة عملاء** | الرد على واتساب، شكاوى | `whatsapp-commerce.ts` |
| **محاسب** | تقارير مالية، COD | `financial.ts` |
| **مدير مخزون** | إدارة المخزون | `inventory.ts` |
| **مندوب توصيل** | تحديث الشحنات | `shipments.ts` |
| **مشرف** | كل الصلاحيات | `admin.ts` |

### API Endpoints

```typescript
// تسجيل دخول الموظف
POST /api/trpc/employeeAuth.login
{
  employeeCode: "EMP001",
  password: "xxxxx"
}

// تسجيل مكالمة
POST /api/trpc/orders.recordCall
{
  customerPhone: "01012345678",
  outcome: "sale", // أو "followup", "no_answer"
  notes: "العميل مهتم بالمنتج"
}

// جدول المتابعات
GET /api/trpc/orders.getFollowups?input={"employeeId":"EMP001"}
```

### لوحة تحكم الموظف

```
/employee/dashboard    - لوحة التحكم
/employee/calls        - سجل المكالمات
/employee/orders       - طلباتي
/employee/performance  - أدائي
/employee/schedule     - جدول المتابعات
```

---

## 📣 خدمات المسوّق (Marketer)

### الوصف
المسوّق يدير الحملات التسويقية والعروض الترويجية.

### الخدمات المتاحة

| الخدمة | الوصف | الـ Router |
|--------|-------|------------|
| **إدارة الحملات** | إنشاء وتتبع الحملات | `campaigns.ts` |
| **بث واتساب** | رسائل جماعية | `whatsapp-commerce.ts` |
| **تقسيم العملاء** | Segmentation | `messaging.ts` |
| **تحليل الأداء** | ROI, Conversions | `bio-dashboard.ts` |
| **كوبونات الخصم** | إدارة العروض | `products.ts` |
| **المحتوى** | إنشاء محتوى AI | `contentCreator.ts` |

### API Endpoints

```typescript
// إنشاء حملة واتساب
POST /api/trpc/whatsappCommerce.createBroadcast
{
  name: "عروض الصيف",
  segment: "vip_customers",
  template: "summer_sale",
  scheduledAt: "2024-07-01T10:00:00Z"
}

// إنشاء كوبون
POST /api/trpc/products.createCoupon
{
  code: "SUMMER20",
  discountType: "percentage",
  discountValue: 20,
  expiresAt: "2024-07-31"
}

// تحليل حملة
GET /api/trpc/campaigns.getAnalytics?input={"campaignId":"123"}
```

---

## 👤 خدمات العميل النهائي (End Customer)

### الوصف
العميل النهائي يشتري المنتجات من التاجر.

### قنوات الشراء

| القناة | الوصف |
|--------|-------|
| **الموقع** | التسوق عبر الموقع |
| **واتساب** | إرسال طلب عبر واتساب |
| **الهاتف** | الاتصال بالمبيعات |
| **Shopify** | المتجر الإلكتروني |

### خدمات العميل

```
- تتبع الطلب
- التقسيط (BNPL)
- الدفع المتعدد (COD, بطاقة, محفظة)
- الإرجاع والاستبدال
- خدمة العملاء
```

---

# 4. الوحدات الأساسية

## Orders Module (نظام الطلبات)

### الملفات
```
server/routers/orders.ts
server/services/order.service.ts
drizzle/schema.ts (orders table)
```

### الحالات

```typescript
type OrderStatus =
  | "pending"      // منتظر
  | "confirmed"    // مؤكد
  | "processing"   // قيد التجهيز
  | "shipped"      // تم الشحن
  | "delivered"    // تم التوصيل
  | "cancelled"    // ملغي
  | "returned";    // مرتجع
```

### Workflow

```
┌─────────┐     ┌───────────┐     ┌────────────┐     ┌─────────┐
│ Pending │ ──▶ │ Confirmed │ ──▶ │ Processing │ ──▶ │ Shipped │
└─────────┘     └───────────┘     └────────────┘     └────┬────┘
                                                          │
                    ┌───────────┐     ┌───────────┐       │
                    │ Cancelled │ ◀── │ Returned  │ ◀─────┼───▶ Delivered
                    └───────────┘     └───────────┘       │
                                                          ▼
                                                    ┌───────────┐
                                                    │ Delivered │
                                                    └───────────┘
```

---

## Payment Module (نظام الدفع)

### الملفات
```
server/routers/payment.ts
server/services/unified-payment.service.ts
server/integrations/instapay.ts
server/integrations/mobile-wallets.ts
drizzle/schema-payments.ts
```

### بوابات الدفع المدعومة

| البوابة | النوع | الـ Integration |
|---------|-------|-----------------|
| **COD** | نقداً عند الاستلام | Built-in |
| **InstaPay** | تحويل فوري | `instapay.ts` |
| **PayMob** | بطاقات ائتمان | `paymob.ts` |
| **Fawry** | رقم مرجعي | `fawry.ts` |
| **فودافون كاش** | محفظة | `mobile-wallets.ts` |
| **اتصالات كاش** | محفظة | `mobile-wallets.ts` |
| **أورانج كاش** | محفظة | `mobile-wallets.ts` |

### Unified Payment Interface

```typescript
// استخدام موحد لكل البوابات
const payment = await UnifiedPaymentService.createPayment({
  orderId: 123,
  amount: 599.99,
  provider: "instapay", // أو "paymob", "fawry", etc.
  customerPhone: "01012345678"
});
```

---

## Shipping Module (نظام الشحن)

### الملفات
```
server/routers/shipments.ts
server/integrations/bosta-api.ts
server/integrations/jnt-api.ts
drizzle/schema-shipping.ts
```

### شركات الشحن المدعومة

| الشركة | Coverage | COD |
|--------|----------|-----|
| **Bosta** | كل مصر | ✅ |
| **J&T Express** | كل مصر | ✅ |

### إنشاء شحنة

```typescript
const shipment = await ShipmentsRouter.create({
  orderId: 123,
  provider: "bosta",
  recipientName: "أحمد محمد",
  recipientPhone: "01012345678",
  address: "15 شارع التحرير",
  city: "مدينة نصر",
  governorate: "القاهرة",
  codAmount: 599.99
});

// Returns:
// {
//   trackingNumber: "BST-123456",
//   estimatedDelivery: "2024-01-17"
// }
```

---

## BNPL Module (نظام التقسيط)

### الملفات
```
server/routers/bnpl.ts
drizzle/schema-bnpl.ts
```

### مقدمي الخدمة

| المزود | أقصى مبلغ | أقساط |
|--------|----------|-------|
| **ValU** | 50,000 ج.م | 3-36 شهر |
| **Sympl** | 30,000 ج.م | 3-12 شهر |
| **Souhoola** | 25,000 ج.م | 3-12 شهر |
| **Contact** | 40,000 ج.م | 3-24 شهر |

### Workflow

```
فحص الأهلية ──▶ اختيار الخطة ──▶ توقيع العقد ──▶ تحصيل شهري
```

---

## WhatsApp Commerce Module

### الملفات
```
server/routers/whatsapp-commerce.ts
server/integrations/whatsapp-business.ts
drizzle/schema-whatsapp.ts
```

### الميزات

```
- 📦 إرسال كتالوج المنتجات
- 🛒 إنشاء سلة تسوق
- 📝 إنشاء طلب من المحادثة
- 🤖 ردود آلية ذكية
- 📢 بث رسائل جماعية
- 📊 تحليلات المحادثات
```

---

# 5. Bio-Modules

## نظرة عامة

Bio-Modules هي وحدات ذكاء اصطناعي مستوحاة من الطبيعة:

```
┌─────────────────────────────────────────────────────────────┐
│                    Bio-Modules                              │
├─────────────────────────────────────────────────────────────┤
│  🐻 Tardigrade    │ المرونة - يتحمل الأعطال                 │
│  🦎 Chameleon     │ التكيف - يتكيف مع التغييرات             │
│  🐙 Cephalopod    │ التعلم - يتعلم من البيانات              │
│  🍄 Mycelium      │ الشبكة - يربط الأنظمة                   │
│  🦅 Corvid        │ الذكاء - يحلل ويتوقع                    │
│  🐜 Ant           │ التعاون - يوزع المهام                   │
└─────────────────────────────────────────────────────────────┘
```

### Tardigrade (دب الماء)

**المهمة:** النظام يتحمل الأعطال ويستمر في العمل

```typescript
// server/bio-modules/tardigrade.ts

class TardigradeModule {
  // إعادة المحاولة التلقائية
  async withResilience<T>(operation: () => Promise<T>): Promise<T> {
    return retry(operation, {
      retries: 3,
      backoff: "exponential"
    });
  }

  // التعافي من الأخطاء
  async recover(error: Error): Promise<void> {
    // Log, notify, and attempt recovery
  }
}
```

### Chameleon (الحرباء)

**المهمة:** التكيف مع متغيرات السوق والعملاء

```typescript
// server/bio-modules/chameleon.ts

class ChameleonModule {
  // تكيف الأسعار
  async adaptPricing(product: Product, context: MarketContext): Promise<number> {
    // تحليل المنافسين، الطلب، الموسم
  }

  // تخصيص التجربة
  async personalizeExperience(customer: Customer): Promise<Experience> {
    // تحليل السلوك السابق
  }
}
```

### Cephalopod (الأخطبوط)

**المهمة:** التعلم من البيانات والتنبؤ

```typescript
// server/bio-modules/cephalopod.ts

class CephalopodModule {
  // توقع الطلب
  async predictDemand(product: Product): Promise<DemandForecast> {
    // Machine Learning model
  }

  // اكتشاف الأنماط
  async detectPatterns(data: DataSet): Promise<Pattern[]> {
    // Pattern recognition
  }
}
```

---

# 6. التكاملات الخارجية

## Shopify Integration

### الإعداد

```bash
# Environment Variables
SHOPIFY_SHOP_URL="your-store.myshopify.com"
SHOPIFY_ACCESS_TOKEN="shpat_xxxxx"
SHOPIFY_WEBHOOK_SECRET="whsec_xxxxx"
```

### Webhooks المدعومة

| Event | Handler |
|-------|---------|
| `orders/create` | إنشاء طلب محلي |
| `orders/updated` | تحديث الطلب |
| `orders/cancelled` | إلغاء الطلب |
| `products/create` | مزامنة المنتج |
| `products/update` | تحديث المنتج |

### الاستخدام

```typescript
// مزامنة المنتجات
await shopifyRouter.syncProducts();

// مزامنة الطلبات
await shopifyRouter.syncOrders({ since: "2024-01-01" });
```

---

## Bosta Integration

### الإعداد

```bash
BOSTA_API_KEY="your_bosta_api_key"
BOSTA_WEBHOOK_SECRET="your_webhook_secret"
```

### الاستخدام

```typescript
import { BostaAPI } from "../integrations/bosta-api";

// إنشاء شحنة
const shipment = await BostaAPI.createDelivery({
  type: "SEND",
  specs: {
    packageDetails: { itemsCount: 1, description: "طلب #123" },
    size: "SMALL",
    weight: 1
  },
  receiver: {
    firstName: "أحمد",
    lastName: "محمد",
    phone: "01012345678"
  },
  dropOffAddress: {
    city: "Cairo",
    zone: "Nasr City",
    firstLine: "15 شارع التحرير"
  },
  cod: 599.99
});
```

---

# 7. دليل التطوير

## إعداد بيئة التطوير

```bash
# 1. Clone the repository
git clone https://github.com/ka364/HADEROS-AI-CLOUD.git
cd HADEROS-AI-CLOUD

# 2. Install dependencies
cd apps/haderos-web
pnpm install

# 3. Setup environment
cp .env.production.ready .env
# Edit .env with your values

# 4. Generate security keys
cd ../..
./scripts/generate-secrets.sh

# 5. Setup database
pnpm drizzle-kit push

# 6. Start development server
pnpm dev
```

## إضافة Router جديد

```typescript
// server/routers/my-feature.ts

import { z } from "zod";
import { router, publicProcedure, protectedProcedure } from "../_core/trpc";
import { db } from "../db";

export const myFeatureRouter = router({
  // Query (GET)
  list: protectedProcedure
    .input(z.object({ limit: z.number().optional() }))
    .query(async ({ input }) => {
      // Implementation
    }),

  // Mutation (POST)
  create: protectedProcedure
    .input(z.object({
      name: z.string(),
      value: z.number()
    }))
    .mutation(async ({ input }) => {
      // Implementation
    }),
});
```

ثم أضفه في `routers.ts`:

```typescript
import { myFeatureRouter } from "./routers/my-feature";

export const appRouter = router({
  // ... existing routers
  myFeature: myFeatureRouter,
});
```

## إضافة Schema جديد

```typescript
// drizzle/schema-my-feature.ts

import { pgTable, serial, varchar, timestamp } from "drizzle-orm/pg-core";

export const myFeatures = pgTable("my_features", {
  id: serial("id").primaryKey(),
  name: varchar("name", { length: 200 }).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});
```

ثم:

```bash
pnpm drizzle-kit push
```

## إضافة Integration جديد

```typescript
// server/integrations/new-service.ts

export class NewServiceAPI {
  private apiKey: string;
  private baseUrl: string;

  constructor() {
    this.apiKey = process.env.NEW_SERVICE_API_KEY!;
    this.baseUrl = "https://api.newservice.com";
  }

  async makeRequest(endpoint: string, data: any): Promise<any> {
    const response = await fetch(`${this.baseUrl}${endpoint}`, {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${this.apiKey}`,
        "Content-Type": "application/json"
      },
      body: JSON.stringify(data)
    });
    return response.json();
  }
}
```

---

# 8. دليل التشغيل

## التشغيل المحلي

```bash
# Development
pnpm dev

# Production build
pnpm build
pnpm start
```

## التشغيل على الخادم

### باستخدام PM2

```bash
# Install PM2
npm install -g pm2

# Start application
pm2 start pnpm --name "haderos" -- start

# View logs
pm2 logs haderos

# Restart
pm2 restart haderos

# Stop
pm2 stop haderos
```

### باستخدام Docker

```bash
# Build
docker build -t haderos .

# Run
docker run -d \
  -p 3000:3000 \
  -e DATABASE_URL="..." \
  -e JWT_SECRET="..." \
  --name haderos \
  haderos
```

## Environment Variables

```bash
# Required
DATABASE_URL="postgresql://user:pass@host:5432/db"
JWT_SECRET="generated_secret"
SESSION_SECRET="generated_secret"

# Shopify (optional)
SHOPIFY_SHOP_URL="store.myshopify.com"
SHOPIFY_ACCESS_TOKEN="shpat_xxx"

# Shipping (optional)
BOSTA_API_KEY="xxx"
JNT_API_KEY="xxx"

# Payment (optional)
INSTAPAY_API_KEY="xxx"
PAYMOB_API_KEY="xxx"
FAWRY_MERCHANT_CODE="xxx"

# WhatsApp (optional)
WHATSAPP_ACCESS_TOKEN="xxx"
WHATSAPP_PHONE_NUMBER_ID="xxx"
```

## Health Check

```bash
# Run health check
./scripts/health-check.sh

# Or via API
curl http://localhost:3000/api/health
```

---

# 9. الأمان

## المصادقة

```typescript
// JWT-based authentication
const token = jwt.sign({ userId, role }, JWT_SECRET, { expiresIn: "7d" });
```

## الصلاحيات

| Role | Permissions |
|------|-------------|
| `admin` | كل الصلاحيات |
| `manager` | إدارة بدون حذف |
| `employee` | عمليات محدودة |
| `viewer` | قراءة فقط |

## Webhook Security

```typescript
// Verify Shopify webhook
const isValid = verifyShopifyWebhook(
  req.body,
  req.headers["x-shopify-hmac-sha256"],
  SHOPIFY_WEBHOOK_SECRET
);
```

## Rate Limiting

```typescript
// Built-in rate limiting
app.use(rateLimit({
  windowMs: 60 * 1000, // 1 minute
  max: 100 // 100 requests per minute
}));
```

---

# 10. استكشاف الأخطاء

## أخطاء شائعة

### Database Connection Error

```
Error: connect ECONNREFUSED
```

**الحل:**
```bash
# تأكد من تشغيل PostgreSQL
pg_isready -h localhost -p 5432

# تحقق من DATABASE_URL
echo $DATABASE_URL
```

### Build Errors

```
Error: Could not resolve "..."
```

**الحل:**
```bash
# أعد تثبيت الحزم
rm -rf node_modules
pnpm install
```

### API Errors

```
TRPCError: UNAUTHORIZED
```

**الحل:**
```typescript
// تأكد من إرسال الـ token
headers: {
  "Authorization": `Bearer ${token}`
}
```

## Logs

```bash
# Application logs
pm2 logs haderos

# Database logs
tail -f /var/log/postgresql/postgresql-*.log
```

---

# الملحقات

## روابط مفيدة

- [Postman Collection](./HADEROS_API_Postman_Collection.json)
- [Quick Start](./QUICK_START_AR.md)
- [API Reference](./API_REFERENCE_AR.md)
- [Deployment Guide](./DEPLOYMENT_GUIDE_AR.md)
- [Troubleshooting](./TROUBLESHOOTING_AR.md)

## الدعم الفني

- 📧 support@haderos.ai
- 📱 01000000000
- 🐛 [GitHub Issues](https://github.com/ka364/HADEROS-AI-CLOUD/issues)

---

**HADEROS AI Cloud** - المرجع الفني الشامل

*إصدار 1.0.0 - يناير 2026*
