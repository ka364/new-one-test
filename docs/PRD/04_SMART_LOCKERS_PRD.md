# Product Requirements Document (PRD)
# نظام الخزائن الذكية - Smart Lockers System

---

## معلومات المستند

| البند | القيمة |
|-------|--------|
| **رقم المستند** | PRD-HADEROS-SL-004 |
| **الإصدار** | 1.0 |
| **تاريخ الإنشاء** | 2 يناير 2026 |
| **الحالة** | مسودة للمراجعة |

---

## 1. نظرة عامة (Overview)

### 1.1 ملخص تنفيذي
نظام الخزائن الذكية يوفر نقاط استلام آمنة على مدار الساعة، حيث يمكن للعملاء استلام طلباتهم في أي وقت يناسبهم دون الحاجة لانتظار المندوب أو التواجد في المنزل.

### 1.2 لماذا مهم لمصر؟

```
🏠 مشكلة "مش في البيت"
   └── 40% من محاولات التوصيل الأولى تفشل
   └── تكلفة إعادة التوصيل: 15-30 جنيه

🏢 طبيعة العمل في مصر
   └── أغلب الناس في العمل من 9 صباحاً لـ 5 مساءً
   └── مواعيد التوصيل غالباً خلال ساعات العمل

🔐 مخاوف أمنية
   └── عدم الراحة لترك الطلبات مع البواب
   └── سرقات الطلبات من الأبواب

💰 توفير التكاليف
   └── توصيل لخزانة واحدة vs 50 عنوان منفصل
```

### 1.3 أنواع الخزائن

| النوع | الموقع | السعة | الميزات |
|-------|--------|-------|---------|
| **Indoor Lockers** | مولات، سوبر ماركت | 50-200 خانة | مكيفة، تبريد متاح |
| **Outdoor Lockers** | محطات مترو، شوارع | 30-100 خانة | مقاومة للعوامل الجوية |
| **Compact Lockers** | محلات صغيرة، صيدليات | 10-30 خانة | حجم صغير، تكلفة أقل |
| **Refrigerated Lockers** | قرب السوبر ماركت | 20-50 خانة | للمنتجات الغذائية |
| **Parcel Towers** | مجمعات سكنية كبيرة | 100-300 خانة | برج متعدد الطوابق |

### 1.4 الجمهور المستهدف

**المستخدمون النهائيون:**
| الشريحة | الاحتياج | الموقع المفضل |
|---------|----------|---------------|
| موظفو الشركات | استلام أثناء العمل | مولات قريبة من المكاتب |
| طلاب الجامعات | استلام بين المحاضرات | داخل الحرم الجامعي |
| سكان الكمبوندات | خصوصية وأمان | بوابة الكمبوند |
| مستخدمو المترو | استلام في الطريق | محطات المترو |
| مشترو المنتجات الحساسة | خصوصية تامة | أي موقع بعيد عن البيت |

**الشركاء المحتملون:**
| الشريك | القيمة المضافة | نموذج الشراكة |
|--------|---------------|---------------|
| كارفور/سبينس | مواقع ممتازة، مبردات | إيجار مساحة |
| محطات مترو القاهرة | حركة مرور عالية | شراكة حكومية |
| محطات بنزين | متاحة 24/7 | نسبة من الإيرادات |
| الجامعات | جمهور شاب، كثيف | اتفاقية خدمة |
| شركات إدارة الكمبوندات | خدمة للسكان | رسوم اشتراك |

---

## 2. المتطلبات الوظيفية (Functional Requirements)

### 2.1 نظام الخزائن (Locker Management)

#### FR-SL-001: إدارة الخزائن والمواقع
**الوصف:** نظام مركزي لإدارة جميع الخزائن ومراقبة حالتها

**البيانات المطلوبة:**
```typescript
interface LockerStation {
  id: string;
  code: string;                    // SL-CAI-001 (Smart Locker - Cairo - 001)
  name: string;                    // "خزائن سيتي ستارز"
  nameAr: string;
  type: 'indoor' | 'outdoor' | 'compact' | 'refrigerated' | 'tower';

  location: {
    address: string;
    addressAr: string;
    governorate: string;           // القاهرة
    district: string;              // مدينة نصر
    landmark: string;              // "بجوار البوابة الرئيسية"
    coordinates: {
      latitude: number;
      longitude: number;
    };
    accessInstructions: string;    // "ادخل من البوابة 3"
    accessInstructionsAr: string;
  };

  partner: {
    id: string;
    name: string;
    type: 'mall' | 'supermarket' | 'metro' | 'gas_station' | 'university' | 'compound';
    contactPerson: string;
    contactPhone: string;
  };

  operatingHours: {
    is24Hours: boolean;
    schedule?: {
      [day: string]: {
        open: string;    // "08:00"
        close: string;   // "23:00"
      };
    };
  };

  capacity: {
    total: number;
    small: number;       // للطرود الصغيرة
    medium: number;      // للطرود المتوسطة
    large: number;       // للطرود الكبيرة
    xlarge: number;      // للطرود الضخمة
    refrigerated: number; // للمنتجات المبردة
  };

  features: {
    hasRefrigeration: boolean;
    hasCamera: boolean;
    hasIntercom: boolean;
    hasAccessibility: boolean;     // وصول ذوي الاحتياجات
    hasPrinter: boolean;           // طباعة إيصالات
    hasPaymentTerminal: boolean;   // دفع عند الاستلام
  };

  status: 'active' | 'maintenance' | 'offline' | 'full';
  healthStatus: {
    lastPing: Date;
    connectivity: 'online' | 'degraded' | 'offline';
    temperature?: number;          // للمبردات
    powerStatus: 'normal' | 'backup' | 'critical';
  };

  statistics: {
    utilizationRate: number;       // نسبة الاستخدام
    averageDwellTime: number;      // متوسط وقت بقاء الطرد (ساعات)
    pickupsToday: number;
    depositsToday: number;
  };

  createdAt: Date;
  updatedAt: Date;
}

interface LockerCell {
  id: string;
  stationId: string;
  cellNumber: string;              // A-01, B-15, etc.
  size: 'small' | 'medium' | 'large' | 'xlarge' | 'refrigerated';

  dimensions: {
    width: number;                 // cm
    height: number;
    depth: number;
    maxWeight: number;             // kg
  };

  status: 'available' | 'occupied' | 'reserved' | 'maintenance' | 'broken';
  currentReservation?: {
    orderId: string;
    customerId: string;
    reservedAt: Date;
    expiresAt: Date;
    accessCode: string;
  };

  hardware: {
    lockStatus: 'locked' | 'unlocked';
    doorStatus: 'open' | 'closed';
    sensorStatus: 'ok' | 'fault';
    lastOpened?: Date;
  };

  pricing: {
    first24Hours: number;          // مجاناً أو رسوم رمزية
    additional24Hours: number;     // رسوم التأخير
    maxStorageDays: number;        // الحد الأقصى للتخزين
  };
}
```

**واجهة لوحة التحكم المركزية:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  HADEROS Smart Lockers - لوحة التحكم المركزية                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  📊 الإحصائيات اللحظية                                              │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐         │
│  │ إجمالي      │ متاحة       │ مشغولة      │ صيانة       │         │
│  │ الخزائن     │             │             │             │         │
│  │   5,420     │   3,250     │   2,100     │     70      │         │
│  │  100 موقع   │    60%      │    39%      │     1%      │         │
│  └─────────────┴─────────────┴─────────────┴─────────────┘         │
│                                                                     │
│  🗺️ خريطة المواقع                                                   │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │                    [خريطة تفاعلية]                      │       │
│  │                                                         │       │
│  │    🟢 سيتي ستارز (45 متاحة)                             │       │
│  │    🟡 مول مصر (12 متاحة)                                │       │
│  │    🔴 محطة السادات (ممتلئة)                             │       │
│  │    🟢 كارفور المعادي (78 متاحة)                         │       │
│  │                                                         │       │
│  └─────────────────────────────────────────────────────────┘       │
│                                                                     │
│  ⚠️ تنبيهات تحتاج انتباه                                            │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │  🔴 محطة العتبة - اتصال مقطوع منذ 15 دقيقة              │       │
│  │  🟠 خزانة A-15 بمول العرب - مستشعر معطل                │       │
│  │  🟡 12 طرد متأخر عن الاستلام > 48 ساعة                  │       │
│  └─────────────────────────────────────────────────────────┘       │
│                                                                     │
│  [إضافة موقع جديد] [تقارير] [إعدادات] [دعم فني]                    │
└─────────────────────────────────────────────────────────────────────┘
```

#### FR-SL-002: حجز الخزانة عند الطلب
**الوصف:** عند الشراء، يختار العميل الاستلام من خزانة ذكية

**سير العمل:**
```
┌─────────────────────────────────────────────────────────────────────┐
│                        رحلة اختيار الخزانة                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1️⃣ إتمام عملية الشراء                                             │
│      ├── العميل يضيف المنتجات للسلة                                │
│      └── ينتقل لصفحة الدفع                                         │
│             ↓                                                       │
│  2️⃣ اختيار طريقة التوصيل                                          │
│      ┌────────────────────────────────────────────┐                │
│      │  ○ توصيل للمنزل (25-35 ج.م)               │                │
│      │  ● استلام من خزانة ذكية (10 ج.م) 💡        │ ← العميل يختار│
│      │  ○ استلام من المتجر (مجاني)               │                │
│      └────────────────────────────────────────────┘                │
│             ↓                                                       │
│  3️⃣ اختيار موقع الخزانة                                           │
│      ┌────────────────────────────────────────────┐                │
│      │  📍 الخزائن القريبة منك                    │                │
│      │                                            │                │
│      │  [🟢] سيتي ستارز - 1.2 كم                  │                │
│      │       45 خانة متاحة | 24/7                 │                │
│      │       [اختيار]                              │                │
│      │                                            │                │
│      │  [🟢] كارفور المعادي - 2.5 كم              │                │
│      │       32 خانة متاحة | 8ص-12م               │                │
│      │       [اختيار]                              │                │
│      │                                            │                │
│      │  [🟡] محطة مترو السادات - 3.1 كم           │                │
│      │       5 خانات متاحة | 6ص-12م               │                │
│      │       [اختيار]                              │                │
│      └────────────────────────────────────────────┘                │
│             ↓                                                       │
│  4️⃣ تأكيد الحجز                                                    │
│      ├── النظام يحجز خانة بالحجم المناسب                           │
│      ├── يولد كود استلام فريد                                      │
│      └── يرسل تفاصيل للعميل (SMS + إشعار)                          │
│             ↓                                                       │
│  5️⃣ وضع الطرد بالخزانة                                            │
│      ├── المندوب يصل للخزانة                                       │
│      ├── يسكان كود الطرد                                           │
│      ├── الخزانة تفتح تلقائياً                                     │
│      └── يضع الطرد ويغلق الباب                                     │
│             ↓                                                       │
│  6️⃣ إشعار العميل                                                   │
│      ├── إشعار فوري: "طردك جاهز للاستلام!"                         │
│      ├── كود الاستلام: XXXX-XXXX                                   │
│      ├── الموقع والتوجيهات                                         │
│      └── صلاحية الاستلام: 72 ساعة                                  │
│             ↓                                                       │
│  7️⃣ استلام العميل                                                  │
│      ├── يذهب للخزانة في أي وقت                                    │
│      ├── يدخل الكود أو يسكان QR                                    │
│      ├── الباب يفتح تلقائياً                                       │
│      └── يأخذ الطرد - تم!                                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**كود الحجز:**
```typescript
interface LockerReservation {
  id: string;
  orderId: string;
  customerId: string;

  station: {
    id: string;
    name: string;
    address: string;
    coordinates: { lat: number; lng: number };
  };

  cell: {
    id: string;
    number: string;
    size: string;
  };

  accessCode: {
    code: string;                  // 8-digit numeric
    qrCode: string;                // QR code data
    expiresAt: Date;
  };

  timeline: {
    reservedAt: Date;              // وقت الحجز
    expectedDepositAt: Date;       // الوقت المتوقع للإيداع
    depositedAt?: Date;            // وقت الإيداع الفعلي
    expiresAt: Date;               // انتهاء صلاحية الاستلام
    pickedUpAt?: Date;             // وقت الاستلام
    returnedAt?: Date;             // إذا تم إرجاع الطرد
  };

  status:
    | 'reserved'                   // محجوز، في انتظار الإيداع
    | 'deposited'                  // الطرد بالخزانة
    | 'picked_up'                  // تم الاستلام
    | 'expired'                    // انتهت الصلاحية
    | 'returned_to_sender';        // أُعيد للمرسل

  fees: {
    deliveryFee: number;           // رسوم التوصيل للخزانة
    storageFee: number;            // رسوم التخزين الإضافية
    totalPaid: number;
  };

  notifications: {
    depositedSent: boolean;
    reminder24hSent: boolean;
    reminder48hSent: boolean;
    expiringSoonSent: boolean;
  };
}

async function reserveLocker(orderId: string, stationId: string): Promise<LockerReservation> {
  // 1. الحصول على تفاصيل الطلب
  const order = await getOrder(orderId);

  // 2. تحديد حجم الخانة المطلوبة
  const requiredSize = calculateRequiredSize(order.items);

  // 3. البحث عن خانة متاحة
  const availableCell = await findAvailableCell(stationId, requiredSize);
  if (!availableCell) {
    throw new Error('لا توجد خانات متاحة بالحجم المطلوب');
  }

  // 4. توليد كود الاستلام
  const accessCode = generateSecureAccessCode();

  // 5. إنشاء الحجز
  const reservation = await db.lockerReservations.create({
    orderId,
    customerId: order.customerId,
    stationId,
    cellId: availableCell.id,
    accessCode: {
      code: accessCode,
      qrCode: generateQRCode(accessCode),
      expiresAt: addHours(new Date(), 72),
    },
    timeline: {
      reservedAt: new Date(),
      expectedDepositAt: addHours(new Date(), 24),
      expiresAt: addHours(new Date(), 72),
    },
    status: 'reserved',
  });

  // 6. تحديث حالة الخانة
  await db.lockerCells.update(availableCell.id, {
    status: 'reserved',
    currentReservation: {
      orderId,
      customerId: order.customerId,
      reservedAt: new Date(),
      expiresAt: reservation.accessCode.expiresAt,
      accessCode: accessCode,
    },
  });

  // 7. إرسال تأكيد للعميل
  await sendReservationConfirmation(order.customer, reservation);

  return reservation;
}

function calculateRequiredSize(items: OrderItem[]): CellSize {
  const totalVolume = items.reduce((sum, item) => {
    return sum + (item.dimensions.width * item.dimensions.height * item.dimensions.depth * item.quantity);
  }, 0);

  const totalWeight = items.reduce((sum, item) => sum + (item.weight * item.quantity), 0);

  // حدود الأحجام (بالسنتيمتر المكعب والكيلوجرام)
  if (totalVolume <= 15000 && totalWeight <= 3) return 'small';
  if (totalVolume <= 45000 && totalWeight <= 10) return 'medium';
  if (totalVolume <= 120000 && totalWeight <= 25) return 'large';
  return 'xlarge';
}
```

#### FR-SL-003: إيداع الطرد (للمندوب)
**الوصف:** عملية وضع الطرد في الخزانة من قبل مندوب التوصيل

**واجهة تطبيق المندوب:**
```
┌─────────────────────────────────────────────────────────────────────┐
│  تطبيق المندوب - إيداع بالخزانة الذكية                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  📦 الطرد: #ORD-2025-001234                                         │
│  ─────────────────────────────────────────                          │
│  العميل: أحمد محمد                                                  │
│  المنتج: جهاز لابتوب HP                                             │
│                                                                     │
│  📍 الخزانة المحجوزة                                                │
│  ┌─────────────────────────────────────────────────────────┐       │
│  │  سيتي ستارز مول - الطابق الأرضي                         │       │
│  │  بوابة 3، بجوار كارفور                                   │       │
│  │                                                         │       │
│  │  الخانة: B-15 (كبيرة)                                   │       │
│  │                                                         │       │
│  │  [📍 فتح الخرائط]                                        │       │
│  └─────────────────────────────────────────────────────────┘       │
│                                                                     │
│  ═══════════════════════════════════════════════════════════       │
│                                                                     │
│  ✅ خطوات الإيداع:                                                  │
│                                                                     │
│  1. [✓] وصلت للخزانة                                               │
│                                                                     │
│  2. [ ] اسكان كود الطرد                                            │
│     ┌─────────────────────────────────────┐                        │
│     │     [📷 فتح الكاميرا للسكان]        │                        │
│     └─────────────────────────────────────┘                        │
│                                                                     │
│  3. [ ] انتظر فتح الخانة B-15                                      │
│                                                                     │
│  4. [ ] ضع الطرد وأغلق الباب                                       │
│                                                                     │
│  5. [ ] التقط صورة للتأكيد                                         │
│                                                                     │
│                                                                     │
│  ⚠️ مشكلة؟  [الخانة لا تفتح] [حجم غير مناسب] [اتصل بالدعم]         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**كود الإيداع:**
```typescript
async function depositParcel(
  courierId: string,
  orderId: string,
  cellId: string,
  photoProof: string
): Promise<DepositResult> {
  // 1. التحقق من الحجز
  const reservation = await db.lockerReservations.findOne({
    orderId,
    cellId,
    status: 'reserved',
  });

  if (!reservation) {
    throw new Error('لا يوجد حجز نشط لهذا الطلب');
  }

  // 2. فتح الخانة
  const unlockResult = await lockerHardwareApi.unlockCell(cellId);
  if (!unlockResult.success) {
    throw new Error('فشل في فتح الخانة');
  }

  // 3. انتظار وضع الطرد وإغلاق الباب
  await waitForDoorClose(cellId, { timeout: 120000 }); // 2 دقائق

  // 4. التحقق من وجود الطرد بالمستشعر
  const sensorReading = await lockerHardwareApi.checkOccupancy(cellId);
  if (!sensorReading.isOccupied) {
    throw new Error('لم يُكتشف طرد في الخانة');
  }

  // 5. قفل الخانة
  await lockerHardwareApi.lockCell(cellId);

  // 6. تحديث الحجز
  await db.lockerReservations.update(reservation.id, {
    status: 'deposited',
    'timeline.depositedAt': new Date(),
    depositedBy: courierId,
    depositPhoto: photoProof,
  });

  // 7. إشعار العميل
  await notifyCustomerParcelReady(reservation);

  // 8. تسجيل العملية
  await logLockerEvent({
    type: 'deposit',
    reservationId: reservation.id,
    cellId,
    courierId,
    timestamp: new Date(),
  });

  return {
    success: true,
    message: 'تم إيداع الطرد بنجاح',
    customerNotified: true,
  };
}
```

#### FR-SL-004: استلام الطرد (للعميل)
**الوصف:** عملية استلام العميل لطرده من الخزانة

**واجهة شاشة الخزانة (Kiosk):**
```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                    ╔═══════════════════════════╗                    │
│                    ║     مرحباً بك في          ║                    │
│                    ║   خزائن HADEROS الذكية    ║                    │
│                    ╚═══════════════════════════╝                    │
│                                                                     │
│                                                                     │
│         ┌─────────────────────────────────────────────┐            │
│         │                                             │            │
│         │           أدخل كود الاستلام                 │            │
│         │                                             │            │
│         │    ┌───┬───┬───┬───┬───┬───┬───┬───┐      │            │
│         │    │ 1 │ 2 │ 3 │ 4 │ - │ 5 │ 6 │ 7 │      │            │
│         │    └───┴───┴───┴───┴───┴───┴───┴───┘      │            │
│         │                                             │            │
│         │    [  تأكيد  ]     [  مسح  ]               │            │
│         │                                             │            │
│         └─────────────────────────────────────────────┘            │
│                                                                     │
│                           ─── أو ───                               │
│                                                                     │
│         ┌─────────────────────────────────────────────┐            │
│         │                                             │            │
│         │      📱 امسح كود QR من هاتفك              │            │
│         │                                             │            │
│         │         [    📷 ماسح QR     ]              │            │
│         │                                             │            │
│         └─────────────────────────────────────────────┘            │
│                                                                     │
│                                                                     │
│    🌐 English | العربية | 🆘 مساعدة | ☎️ اتصل بالدعم              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**بعد إدخال الكود الصحيح:**
```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                         ✅ تم التحقق!                              │
│                                                                     │
│         ┌─────────────────────────────────────────────┐            │
│         │                                             │            │
│         │    📦 طردك جاهز للاستلام                    │            │
│         │                                             │            │
│         │    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │            │
│         │                                             │            │
│         │    📍 الخانة: B-15                          │            │
│         │       (الصف الثاني، الخامس من اليسار)       │            │
│         │                                             │            │
│         │         ┌─────────────────────┐             │            │
│         │         │    ◀ B-15 ▶        │             │            │
│         │         │    (مُضاءة)         │             │            │
│         │         └─────────────────────┘             │            │
│         │                                             │            │
│         │    🔓 الخانة مفتوحة الآن!                   │            │
│         │       لديك 60 ثانية لأخذ طردك              │            │
│         │                                             │            │
│         │              [  52  ثانية  ]                │            │
│         │                                             │            │
│         └─────────────────────────────────────────────┘            │
│                                                                     │
│              ⚠️ لا تنس إغلاق الباب بعد أخذ الطرد                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**كود الاستلام:**
```typescript
async function pickupParcel(accessCode: string): Promise<PickupResult> {
  // 1. البحث عن الحجز بالكود
  const reservation = await db.lockerReservations.findOne({
    'accessCode.code': accessCode,
    status: 'deposited',
  });

  if (!reservation) {
    // تسجيل محاولة فاشلة
    await logFailedPickupAttempt(accessCode);
    throw new Error('كود غير صحيح أو الطرد غير موجود');
  }

  // 2. التحقق من عدم انتهاء الصلاحية
  if (new Date() > reservation.accessCode.expiresAt) {
    throw new Error('انتهت صلاحية كود الاستلام');
  }

  // 3. فتح الخانة
  const cell = await db.lockerCells.findById(reservation.cellId);
  const unlockResult = await lockerHardwareApi.unlockCell(cell.id);

  if (!unlockResult.success) {
    throw new Error('فشل في فتح الخانة، يرجى الاتصال بالدعم');
  }

  // 4. تشغيل ضوء الخانة
  await lockerHardwareApi.highlightCell(cell.id);

  // 5. انتظار إغلاق الباب
  const doorClosed = await waitForDoorClose(cell.id, { timeout: 120000 });

  // 6. التحقق من أخذ الطرد
  const sensorReading = await lockerHardwareApi.checkOccupancy(cell.id);

  if (sensorReading.isOccupied) {
    // الطرد لا يزال موجوداً
    await lockerHardwareApi.lockCell(cell.id);
    throw new Error('يبدو أنك لم تأخذ الطرد، الخانة مقفلة مجدداً');
  }

  // 7. تحديث الحجز
  await db.lockerReservations.update(reservation.id, {
    status: 'picked_up',
    'timeline.pickedUpAt': new Date(),
  });

  // 8. تحرير الخانة
  await db.lockerCells.update(cell.id, {
    status: 'available',
    currentReservation: null,
  });

  // 9. تسجيل العملية
  await logLockerEvent({
    type: 'pickup',
    reservationId: reservation.id,
    cellId: cell.id,
    timestamp: new Date(),
  });

  return {
    success: true,
    message: 'شكراً لك! استمتع بمشترياتك',
    orderId: reservation.orderId,
  };
}
```

#### FR-SL-005: نظام التنبيهات والتذكير
**الوصف:** إرسال تنبيهات للعميل بخصوص طرده

```typescript
interface NotificationSchedule {
  triggers: {
    // عند الإيداع
    onDeposit: {
      channels: ['sms', 'push', 'whatsapp'];
      message: 'طردك جاهز للاستلام من {station_name}، الخانة {cell_number}. كود الاستلام: {access_code}';
    };

    // تذكير بعد 24 ساعة
    after24Hours: {
      channels: ['push', 'whatsapp'];
      message: 'تذكير: طردك لا يزال في انتظارك بخزانة {station_name}. صالح لمدة {remaining_hours} ساعة أخرى.';
    };

    // تذكير بعد 48 ساعة
    after48Hours: {
      channels: ['sms', 'push', 'whatsapp'];
      message: 'آخر تذكير! طردك سينتهي صلاحيته خلال {remaining_hours} ساعة. استلمه الآن من {station_name}.';
    };

    // قبل انتهاء الصلاحية بساعتين
    beforeExpiry: {
      channels: ['sms', 'push', 'whatsapp', 'call'];
      message: 'عاجل! طردك سينتهي خلال ساعتين وسيُعاد للمرسل. استلمه الآن! كود: {access_code}';
    };

    // عند انتهاء الصلاحية
    onExpiry: {
      channels: ['sms', 'push'];
      message: 'للأسف، انتهت صلاحية استلام طردك وسيُعاد للمرسل. سنتواصل معك بخصوص إعادة التوصيل.';
    };
  };
}

async function scheduleNotifications(reservation: LockerReservation): Promise<void> {
  const { depositedAt, expiresAt } = reservation.timeline;

  // تذكير بعد 24 ساعة
  await scheduleNotification({
    reservationId: reservation.id,
    type: 'after24Hours',
    scheduledAt: addHours(depositedAt, 24),
  });

  // تذكير بعد 48 ساعة
  await scheduleNotification({
    reservationId: reservation.id,
    type: 'after48Hours',
    scheduledAt: addHours(depositedAt, 48),
  });

  // قبل الانتهاء بساعتين
  await scheduleNotification({
    reservationId: reservation.id,
    type: 'beforeExpiry',
    scheduledAt: addHours(expiresAt, -2),
  });
}
```

#### FR-SL-006: إدارة الطرود المنتهية الصلاحية
**الوصف:** التعامل مع الطرود التي لم يتم استلامها

```typescript
interface ExpiredParcelHandling {
  workflow: {
    // 1. تحديد الطرود المنتهية
    identification: {
      checkInterval: '1 hour';
      query: 'status = deposited AND expiresAt < NOW()';
    };

    // 2. محاولة التواصل مع العميل
    customerContact: {
      attempts: 3;
      channels: ['call', 'sms', 'whatsapp'];
      offerOptions: ['extend_24h', 'new_delivery', 'refund'];
    };

    // 3. إذا لم يستجب العميل
    noResponse: {
      action: 'return_to_sender';
      notifyCustomer: true;
      chargeReturnFee: true;
      returnFeeAmount: 25;
    };

    // 4. جمع الطرود المنتهية
    collection: {
      frequency: 'daily';
      time: '6:00 AM';
      assignedTo: 'return_courier';
    };
  };
}

async function handleExpiredParcels(): Promise<void> {
  // البحث عن الحجوزات المنتهية
  const expiredReservations = await db.lockerReservations.find({
    status: 'deposited',
    'timeline.expiresAt': { $lt: new Date() },
  });

  for (const reservation of expiredReservations) {
    // إرسال إشعار للعميل
    await sendNotification(reservation.customerId, {
      type: 'expiry',
      message: 'انتهت صلاحية استلام طردك وسيتم إعادته للمرسل',
      orderId: reservation.orderId,
    });

    // تحديث حالة الحجز
    await db.lockerReservations.update(reservation.id, {
      status: 'expired',
      'timeline.expiredAt': new Date(),
    });

    // إنشاء أمر إرجاع
    await createReturnOrder({
      originalOrderId: reservation.orderId,
      lockerCellId: reservation.cellId,
      reason: 'pickup_expired',
      scheduledCollection: getNextCollectionTime(),
    });

    // إبقاء الخانة مشغولة حتى الجمع
    await db.lockerCells.update(reservation.cellId, {
      status: 'pending_return_collection',
    });
  }
}
```

#### FR-SL-007: نظام الإرجاع عبر الخزانة
**الوصف:** إمكانية إرجاع المشتريات عبر الخزانة الذكية

**سير عملية الإرجاع:**
```
┌─────────────────────────────────────────────────────────────────────┐
│                     رحلة إرجاع عبر الخزانة                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1️⃣ طلب الإرجاع                                                    │
│      ├── العميل يفتح التطبيق                                       │
│      ├── يختار الطلب المراد إرجاعه                                 │
│      ├── يحدد سبب الإرجاع                                          │
│      └── يختار "إرجاع عبر خزانة ذكية"                              │
│             ↓                                                       │
│  2️⃣ حجز خانة للإرجاع                                              │
│      ├── النظام يحجز خانة مناسبة                                   │
│      ├── يولد كود إيداع للعميل                                     │
│      ├── يحدد مهلة الإيداع (24 ساعة)                               │
│      └── يرسل التفاصيل للعميل                                      │
│             ↓                                                       │
│  3️⃣ إيداع المرتجع                                                  │
│      ├── العميل يذهب للخزانة                                       │
│      ├── يدخل كود الإيداع                                          │
│      ├── الخانة تفتح                                               │
│      ├── يضع المنتج ويغلق الباب                                    │
│      └── يلتقط صورة (اختياري)                                      │
│             ↓                                                       │
│  4️⃣ جمع المرتجعات                                                  │
│      ├── مندوب الإرجاعات يزور الخزانة                              │
│      ├── يجمع جميع المرتجعات                                       │
│      └── ينقلها لمركز الفحص                                        │
│             ↓                                                       │
│  5️⃣ فحص المرتجع                                                    │
│      ├── فحص حالة المنتج                                           │
│      ├── التحقق من الأهلية للاسترداد                               │
│      └── اتخاذ القرار                                              │
│             ↓                                                       │
│  6️⃣ الاسترداد                                                      │
│      ├── إذا مقبول: استرداد كامل/جزئي                              │
│      └── إذا مرفوض: إرجاع للعميل مع الشرح                          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. مخطط قاعدة البيانات (Database Schema)

```sql
-- جدول محطات الخزائن
CREATE TABLE locker_stations (
  id VARCHAR(36) PRIMARY KEY,
  code VARCHAR(20) NOT NULL UNIQUE,            -- SL-CAI-001
  name VARCHAR(100) NOT NULL,
  name_ar VARCHAR(100) NOT NULL,
  type ENUM('indoor', 'outdoor', 'compact', 'refrigerated', 'tower') NOT NULL,

  -- الموقع
  address VARCHAR(255) NOT NULL,
  address_ar VARCHAR(255) NOT NULL,
  governorate VARCHAR(50) NOT NULL,
  district VARCHAR(50) NOT NULL,
  landmark VARCHAR(255),
  latitude DECIMAL(10, 8) NOT NULL,
  longitude DECIMAL(11, 8) NOT NULL,
  access_instructions TEXT,
  access_instructions_ar TEXT,

  -- الشريك
  partner_id VARCHAR(36) NOT NULL,
  partner_type ENUM('mall', 'supermarket', 'metro', 'gas_station', 'university', 'compound') NOT NULL,

  -- ساعات العمل
  is_24_hours BOOLEAN DEFAULT FALSE,
  operating_hours JSON,                        -- {"saturday": {"open": "08:00", "close": "23:00"}, ...}

  -- السعة
  total_capacity INT NOT NULL,
  small_cells INT DEFAULT 0,
  medium_cells INT DEFAULT 0,
  large_cells INT DEFAULT 0,
  xlarge_cells INT DEFAULT 0,
  refrigerated_cells INT DEFAULT 0,

  -- المميزات
  has_refrigeration BOOLEAN DEFAULT FALSE,
  has_camera BOOLEAN DEFAULT TRUE,
  has_intercom BOOLEAN DEFAULT FALSE,
  has_accessibility BOOLEAN DEFAULT FALSE,
  has_printer BOOLEAN DEFAULT FALSE,
  has_payment_terminal BOOLEAN DEFAULT FALSE,

  -- الحالة
  status ENUM('active', 'maintenance', 'offline', 'full') DEFAULT 'active',
  last_ping TIMESTAMP,
  connectivity ENUM('online', 'degraded', 'offline') DEFAULT 'online',
  power_status ENUM('normal', 'backup', 'critical') DEFAULT 'normal',

  -- الإحصائيات
  utilization_rate DECIMAL(5, 2) DEFAULT 0,
  avg_dwell_time_hours DECIMAL(5, 2) DEFAULT 0,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_location (governorate, district),
  INDEX idx_coordinates (latitude, longitude),
  INDEX idx_status (status),
  INDEX idx_type (type)
);

-- جدول خانات الخزائن
CREATE TABLE locker_cells (
  id VARCHAR(36) PRIMARY KEY,
  station_id VARCHAR(36) NOT NULL,
  cell_number VARCHAR(10) NOT NULL,            -- A-01, B-15
  size ENUM('small', 'medium', 'large', 'xlarge', 'refrigerated') NOT NULL,

  -- الأبعاد
  width_cm DECIMAL(5, 2) NOT NULL,
  height_cm DECIMAL(5, 2) NOT NULL,
  depth_cm DECIMAL(5, 2) NOT NULL,
  max_weight_kg DECIMAL(5, 2) NOT NULL,

  -- الحالة
  status ENUM('available', 'occupied', 'reserved', 'maintenance', 'broken') DEFAULT 'available',
  current_reservation_id VARCHAR(36),

  -- الأجهزة
  lock_status ENUM('locked', 'unlocked') DEFAULT 'locked',
  door_status ENUM('open', 'closed') DEFAULT 'closed',
  sensor_status ENUM('ok', 'fault') DEFAULT 'ok',
  last_opened_at TIMESTAMP,

  -- التسعير
  first_24h_fee DECIMAL(10, 2) DEFAULT 0,
  additional_24h_fee DECIMAL(10, 2) DEFAULT 5,
  max_storage_days INT DEFAULT 7,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (station_id) REFERENCES locker_stations(id) ON DELETE CASCADE,
  UNIQUE KEY uk_station_cell (station_id, cell_number),
  INDEX idx_status (status),
  INDEX idx_size (size)
);

-- جدول حجوزات الخزائن
CREATE TABLE locker_reservations (
  id VARCHAR(36) PRIMARY KEY,
  order_id VARCHAR(36) NOT NULL,
  customer_id VARCHAR(36) NOT NULL,
  station_id VARCHAR(36) NOT NULL,
  cell_id VARCHAR(36) NOT NULL,

  -- كود الوصول
  access_code VARCHAR(20) NOT NULL,
  access_code_qr TEXT,
  access_code_expires_at TIMESTAMP NOT NULL,

  -- الجدول الزمني
  reserved_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  expected_deposit_at TIMESTAMP,
  deposited_at TIMESTAMP,
  deposited_by VARCHAR(36),                    -- courier_id
  deposit_photo TEXT,
  expires_at TIMESTAMP NOT NULL,
  picked_up_at TIMESTAMP,
  returned_at TIMESTAMP,

  -- الحالة
  status ENUM('reserved', 'deposited', 'picked_up', 'expired', 'returned_to_sender') DEFAULT 'reserved',

  -- الرسوم
  delivery_fee DECIMAL(10, 2) DEFAULT 0,
  storage_fee DECIMAL(10, 2) DEFAULT 0,
  total_paid DECIMAL(10, 2) DEFAULT 0,

  -- الإشعارات
  deposited_notification_sent BOOLEAN DEFAULT FALSE,
  reminder_24h_sent BOOLEAN DEFAULT FALSE,
  reminder_48h_sent BOOLEAN DEFAULT FALSE,
  expiring_soon_sent BOOLEAN DEFAULT FALSE,
  expired_notification_sent BOOLEAN DEFAULT FALSE,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (station_id) REFERENCES locker_stations(id),
  FOREIGN KEY (cell_id) REFERENCES locker_cells(id),
  INDEX idx_order (order_id),
  INDEX idx_customer (customer_id),
  INDEX idx_status (status),
  INDEX idx_access_code (access_code),
  INDEX idx_expires_at (expires_at)
);

-- جدول أحداث الخزائن (للتتبع والمراجعة)
CREATE TABLE locker_events (
  id VARCHAR(36) PRIMARY KEY,
  station_id VARCHAR(36) NOT NULL,
  cell_id VARCHAR(36) NOT NULL,
  reservation_id VARCHAR(36),

  event_type ENUM(
    'cell_unlocked',
    'cell_locked',
    'door_opened',
    'door_closed',
    'parcel_deposited',
    'parcel_picked_up',
    'sensor_triggered',
    'maintenance_start',
    'maintenance_end',
    'error_occurred'
  ) NOT NULL,

  performed_by VARCHAR(36),                    -- user_id or courier_id
  performed_by_type ENUM('customer', 'courier', 'admin', 'system'),

  details JSON,
  photo_url TEXT,

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (station_id) REFERENCES locker_stations(id),
  FOREIGN KEY (cell_id) REFERENCES locker_cells(id),
  INDEX idx_station (station_id),
  INDEX idx_cell (cell_id),
  INDEX idx_reservation (reservation_id),
  INDEX idx_event_type (event_type),
  INDEX idx_created_at (created_at)
);

-- جدول شركاء الخزائن
CREATE TABLE locker_partners (
  id VARCHAR(36) PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  name_ar VARCHAR(100) NOT NULL,
  type ENUM('mall', 'supermarket', 'metro', 'gas_station', 'university', 'compound') NOT NULL,

  contact_person VARCHAR(100),
  contact_phone VARCHAR(20),
  contact_email VARCHAR(100),

  contract_start_date DATE,
  contract_end_date DATE,
  revenue_share_percentage DECIMAL(5, 2),      -- نسبة الشريك من الإيرادات
  monthly_rent DECIMAL(10, 2),                 -- أو إيجار شهري ثابت

  status ENUM('active', 'pending', 'suspended', 'terminated') DEFAULT 'pending',

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- جدول طلبات الإرجاع عبر الخزانة
CREATE TABLE locker_returns (
  id VARCHAR(36) PRIMARY KEY,
  original_order_id VARCHAR(36) NOT NULL,
  customer_id VARCHAR(36) NOT NULL,
  station_id VARCHAR(36) NOT NULL,
  cell_id VARCHAR(36),

  -- كود الإيداع
  deposit_code VARCHAR(20) NOT NULL,
  deposit_code_expires_at TIMESTAMP NOT NULL,

  -- الحالة
  status ENUM(
    'pending_deposit',                         -- في انتظار إيداع العميل
    'deposited',                               -- تم الإيداع
    'collected',                               -- تم الجمع
    'inspecting',                              -- قيد الفحص
    'approved',                                -- مقبول للاسترداد
    'rejected',                                -- مرفوض
    'refunded'                                 -- تم الاسترداد
  ) DEFAULT 'pending_deposit',

  return_reason VARCHAR(50),
  return_notes TEXT,

  deposited_at TIMESTAMP,
  collected_at TIMESTAMP,
  collected_by VARCHAR(36),                    -- courier_id
  inspection_result JSON,
  refund_amount DECIMAL(10, 2),

  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (station_id) REFERENCES locker_stations(id),
  INDEX idx_order (original_order_id),
  INDEX idx_customer (customer_id),
  INDEX idx_status (status),
  INDEX idx_deposit_code (deposit_code)
);
```

---

## 4. واجهات برمجة التطبيقات (API Specification)

### 4.1 نقاط النهاية

```typescript
// ==================== Locker Stations ====================

/**
 * @api {get} /api/lockers/stations البحث عن محطات الخزائن
 * @apiQuery {number} latitude - خط العرض
 * @apiQuery {number} longitude - خط الطول
 * @apiQuery {number} [radius=5] - نطاق البحث بالكيلومتر
 * @apiQuery {string} [size] - حجم الخانة المطلوب (small, medium, large)
 * @apiQuery {boolean} [refrigerated] - هل يلزم تبريد
 */
GET /api/lockers/stations

/**
 * @api {get} /api/lockers/stations/:id تفاصيل محطة خزائن
 */
GET /api/lockers/stations/:id

/**
 * @api {get} /api/lockers/stations/:id/availability التحقق من الخانات المتاحة
 * @apiQuery {string} size - حجم الخانة المطلوب
 */
GET /api/lockers/stations/:id/availability


// ==================== Reservations ====================

/**
 * @api {post} /api/lockers/reservations حجز خانة
 * @apiBody {string} orderId - رقم الطلب
 * @apiBody {string} stationId - رقم المحطة
 * @apiBody {string} [preferredSize] - الحجم المفضل
 */
POST /api/lockers/reservations

/**
 * @api {get} /api/lockers/reservations/:id تفاصيل الحجز
 */
GET /api/lockers/reservations/:id

/**
 * @api {post} /api/lockers/reservations/:id/extend تمديد صلاحية الحجز
 * @apiBody {number} hours - عدد ساعات التمديد
 */
POST /api/lockers/reservations/:id/extend

/**
 * @api {delete} /api/lockers/reservations/:id إلغاء الحجز
 */
DELETE /api/lockers/reservations/:id


// ==================== Courier Operations ====================

/**
 * @api {post} /api/lockers/deposit إيداع طرد (للمندوب)
 * @apiBody {string} orderId - رقم الطلب
 * @apiBody {string} cellId - رقم الخانة
 * @apiBody {string} [photoProof] - صورة الإثبات (base64)
 */
POST /api/lockers/deposit

/**
 * @api {get} /api/lockers/courier/pending طرود معلقة للإيداع (للمندوب)
 */
GET /api/lockers/courier/pending


// ==================== Customer Operations ====================

/**
 * @api {post} /api/lockers/pickup استلام الطرد (للعميل)
 * @apiBody {string} accessCode - كود الاستلام
 */
POST /api/lockers/pickup

/**
 * @api {get} /api/lockers/customer/active الحجوزات النشطة (للعميل)
 */
GET /api/lockers/customer/active


// ==================== Returns ====================

/**
 * @api {post} /api/lockers/returns/request طلب إرجاع عبر الخزانة
 * @apiBody {string} orderId - رقم الطلب الأصلي
 * @apiBody {string} stationId - محطة الإرجاع
 * @apiBody {string} reason - سبب الإرجاع
 */
POST /api/lockers/returns/request

/**
 * @api {post} /api/lockers/returns/:id/deposit إيداع المرتجع
 * @apiBody {string} depositCode - كود الإيداع
 */
POST /api/lockers/returns/:id/deposit


// ==================== Admin ====================

/**
 * @api {get} /api/admin/lockers/dashboard لوحة تحكم الخزائن
 */
GET /api/admin/lockers/dashboard

/**
 * @api {post} /api/admin/lockers/stations إضافة محطة جديدة
 */
POST /api/admin/lockers/stations

/**
 * @api {put} /api/admin/lockers/cells/:id/maintenance وضع خانة في الصيانة
 */
PUT /api/admin/lockers/cells/:id/maintenance

/**
 * @api {get} /api/admin/lockers/expired الطرود المنتهية الصلاحية
 */
GET /api/admin/lockers/expired
```

---

## 5. التكامل مع KAIA

```typescript
interface LockerKAIAIntegration {
  // التحقق الشرعي للمنتجات المخزنة
  productCompliance: {
    // لا تخزين منتجات محرمة
    prohibitedCategories: ['alcohol', 'pork', 'gambling'];

    // التحقق قبل الحجز
    preReservationCheck: async (orderId: string) => {
      const order = await getOrder(orderId);
      for (const item of order.items) {
        const compliance = await kaia.checkProduct(item.productId);
        if (!compliance.isHalal) {
          throw new Error(`المنتج ${item.name} غير متوافق شرعياً`);
        }
      }
    };
  };

  // التسعير العادل
  fairPricing: {
    // رسوم التأخير معقولة
    maxLateFeePerDay: 10;  // جنيه
    maxTotalLateFee: 50;   // جنيه

    // لا رسوم على الإلغاء قبل الإيداع
    cancellationPolicy: {
      beforeDeposit: 'free';
      afterDeposit: 'deliveryFeeOnly';
    };
  };

  // الخصوصية
  privacy: {
    // لا الاطلاع على محتوى الطرد
    noContentInspection: true;

    // حماية بيانات العميل
    customerDataProtection: {
      accessCodeEncryption: true;
      eventLogsRetention: '90 days';
    };
  };
}
```

---

## 6. الأمان والخصوصية

### 6.1 أمان الوصول
```typescript
interface LockerSecurityMeasures {
  accessCodes: {
    // كود عشوائي 8 أرقام
    format: '####-####';
    algorithm: 'crypto.randomBytes';

    // صلاحية محدودة
    defaultValidity: '72 hours';
    maxExtension: '48 hours';

    // عدد المحاولات
    maxAttempts: 5;
    lockoutDuration: '30 minutes';
  };

  hardware: {
    // كاميرات مراقبة
    cameras: {
      recordingDuration: '30 days';
      motionActivated: true;
    };

    // حساسات
    sensors: {
      weightSensor: true;           // للتحقق من وجود الطرد
      tamperDetection: true;        // كشف العبث
      temperatureMonitoring: true;  // للمبردات
    };

    // إنذار
    alarms: {
      forcedEntry: true;
      prolongedDoorOpen: true;
      powerFailure: true;
    };
  };

  data: {
    // تشفير البيانات
    encryption: {
      atRest: 'AES-256';
      inTransit: 'TLS 1.3';
    };

    // سجلات التدقيق
    auditLogs: {
      retention: '1 year';
      events: ['access', 'deposit', 'pickup', 'maintenance'];
    };
  };
}
```

---

## 7. مقاييس النجاح (KPIs)

| المقياس | الهدف | طريقة القياس |
|---------|-------|-------------|
| معدل الاستخدام | > 60% | الخانات المشغولة / الإجمالي |
| متوسط وقت الاستلام | < 24 ساعة | من الإيداع للاستلام |
| معدل انتهاء الصلاحية | < 5% | الطرود المنتهية / الإجمالي |
| رضا العملاء | > 4.5/5 | استطلاعات ما بعد الاستلام |
| توفير التكلفة | 40% | مقارنة بالتوصيل المنزلي |
| معدل الأعطال | < 1% | الخانات المعطلة / الإجمالي |
| وقت الإصلاح | < 4 ساعات | من الإبلاغ للإصلاح |

---

## 8. خطة التنفيذ

### المرحلة 1: التجربة (3 أشهر)
- تركيب 5 محطات في القاهرة الكبرى
- المواقع: سيتي ستارز، مول مصر، كارفور المعادي، محطة السادات، جامعة القاهرة
- 250 خانة إجمالي
- اختبار مع 1000 عميل

### المرحلة 2: التوسع (6 أشهر)
- 25 محطة في القاهرة والجيزة
- 1,500 خانة
- إضافة خزائن مبردة
- شراكات مع محطات البنزين

### المرحلة 3: التغطية الشاملة (12 شهر)
- 100 محطة على مستوى الجمهورية
- 6,000 خانة
- تغطية: القاهرة، الإسكندرية، المنصورة، طنطا، أسيوط
- إضافة ميزة الإرجاع الكاملة

---

## 9. تقدير التكاليف

| البند | التكلفة (جنيه) |
|-------|---------------|
| **Hardware (محطة 50 خانة)** | |
| وحدة الخزانة الذكية | 150,000 |
| التركيب والتوصيلات | 15,000 |
| الكاميرات والحساسات | 10,000 |
| **المجموع لكل محطة** | **175,000** |
| | |
| **Software & Integration** | |
| تطوير النظام | 300,000 |
| تكامل مع الأنظمة | 100,000 |
| اختبار وتشغيل | 50,000 |
| **المجموع** | **450,000** |
| | |
| **التشغيل الشهري** | |
| إيجار المواقع (5 محطات) | 25,000 |
| صيانة وقطع غيار | 10,000 |
| كهرباء | 5,000 |
| دعم فني | 15,000 |
| **المجموع الشهري** | **55,000** |

---

## 10. الملاحق

### 10.1 مخطط أبعاد الخانات

| الحجم | العرض | الارتفاع | العمق | الوزن الأقصى | مثال المنتجات |
|-------|-------|---------|-------|-------------|---------------|
| Small | 30 cm | 15 cm | 40 cm | 3 kg | هواتف، إكسسوارات |
| Medium | 40 cm | 30 cm | 50 cm | 10 kg | ملابس، أحذية |
| Large | 50 cm | 45 cm | 60 cm | 25 kg | لابتوب، أجهزة |
| XLarge | 60 cm | 60 cm | 70 cm | 40 kg | شاشات، أثاث صغير |
| Refrigerated | 40 cm | 35 cm | 50 cm | 15 kg | أغذية، أدوية |

### 10.2 رسائل الإشعارات

```
📦 إشعار الإيداع:
"طردك وصل! رقم الطلب: {order_id}
📍 الموقع: {station_name}
🔐 كود الاستلام: {access_code}
⏰ صالح حتى: {expiry_date}
📲 للتوجيهات: {maps_link}"

⏰ تذكير 24 ساعة:
"تذكير: طردك في انتظارك!
📍 {station_name}
🔐 الكود: {access_code}
⏰ متبقي: {remaining_hours} ساعة"

⚠️ تذكير 48 ساعة:
"آخر تذكير! طردك سينتهي صلاحيته قريباً
استلمه من {station_name}
الكود: {access_code}"

🔴 انتهاء الصلاحية:
"للأسف، انتهت صلاحية استلام طردك.
سيتم إعادته للمرسل.
للمساعدة: {support_number}"
```

---

## 11. تحليل المخاطر (Risk Assessment)

| المخاطر | الاحتمال | التأثير | خطة التخفيف |
|---------|----------|---------|-------------|
| تعطل الخزانة (hardware) | متوسط | عالي | صيانة دورية، فريق استجابة سريع، خزائن بديلة |
| انقطاع الكهرباء | منخفض | عالي | UPS لكل وحدة، إشعار فوري للصيانة |
| محاولة سرقة/اقتحام | منخفض | عالي | كاميرات، إنذارات، تصميم مقاوم للتخريب |
| عدم استلام العميل | متوسط | متوسط | تذكيرات متعددة، رسوم تأخير، إرجاع تلقائي |
| ازدحام وقت الذروة | متوسط | منخفض | تحليل الاستخدام، توسيع المواقع المزدحمة |
| مشاكل في التبريد (للمبردات) | منخفض | عالي | مستشعرات حرارة، تنبيهات فورية، نقل الطرود |

---

## 12. خطة الطوارئ (Contingency Plan)

### 12.1 ماذا لو تعطلت الخزانة؟
```
السيناريو: خانة أو محطة كاملة لا تعمل
الإجراءات:
1. إشعار فوري لفريق الصيانة
2. إبلاغ العملاء المتأثرين
3. تحويل الطرود الجديدة لأقرب محطة
4. للطرود الموجودة: استخدام كود الطوارئ للفتح اليدوي
5. هدف الإصلاح: < 4 ساعات
```

### 12.2 ماذا لو انتهت صلاحية الاستلام؟
```
السيناريو: العميل لم يستلم خلال 72 ساعة
الإجراءات:
1. اتصال هاتفي بالعميل
2. عرض تمديد 24 ساعة إضافية
3. عرض تغيير موقع الاستلام
4. إذا لم يستجب: جمع الطرد + رسوم إرجاع
5. إشعار بإمكانية إعادة التوصيل
```

### 12.3 ماذا لو تلف الطرد داخل الخزانة؟
```
السيناريو: الطرد تضرر (حرارة، رطوبة، إلخ)
الإجراءات:
1. تحقيق فوري (سجلات الحرارة، الكاميرات)
2. إذا خطأ الخزانة: تعويض كامل + اعتذار
3. إذا خطأ التغليف: مطالبة التاجر
4. تقرير للتحسين المستقبلي
```

---

## 13. مؤشرات أداء إضافية (Additional KPIs)

| المقياس | التعريف | الهدف | طريقة الحساب |
|---------|---------|-------|--------------|
| **تكلفة لكل استلام** | التكلفة التشغيلية | < 3 ج.م | إجمالي التكاليف ÷ عدد الاستلامات |
| **ROI لكل محطة** | عائد الاستثمار | > 25% سنوياً | (إيرادات - تكاليف) ÷ تكلفة المحطة |
| **معدل التوفر** | وقت عمل الخزائن | > 99% | ساعات العمل ÷ إجمالي الساعات |
| **معدل الاستلام الأول** | استلام قبل انتهاء الصلاحية | > 95% | استلام ناجح ÷ إجمالي الإيداعات |
| **وقت التفاعل** | من الإشعار للاستلام | < 24 ساعة | متوسط الوقت |
| **NPS للخدمة** | رضا المستخدمين | > 60 | استطلاع ما بعد الاستلام |

---

**نهاية المستند**
