# 🚀 دليل البدء السريع - HADEROS AI CLOUD

**آخر تحديث:** 29 ديسمبر 2025
**الحالة:** جاهز للتشغيل المحلي ✅

---

## ⚡ البدء السريع (5 دقائق)

### 1️⃣ التحقق من المتطلبات

```bash
# التحقق من Node.js (يجب أن يكون 20+)
node --version

# التحقق من pnpm
pnpm --version

# التحقق من PostgreSQL
pg_isready -h localhost -p 5432
```

**إذا كان PostgreSQL غير مشتغل:**
```bash
# على macOS
brew services start postgresql@15

# على Linux
sudo systemctl start postgresql
```

### 2️⃣ إعداد قاعدة البيانات

```bash
# الاتصال بـ PostgreSQL
psql -U postgres

# إنشاء قاعدة البيانات
CREATE DATABASE haderos_dev;

# منح الصلاحيات
GRANT ALL PRIVILEGES ON DATABASE haderos_dev TO ahmedmohamedshawkyatta;

# الخروج
\q
```

### 3️⃣ تشغيل التطبيق

```bash
# الانتقال إلى المجلد
cd apps/haderos-web

# تثبيت المكتبات (إذا لم يتم تثبيتها)
pnpm install

# تشغيل التطوير
pnpm dev
```

### 4️⃣ الوصول إلى التطبيق

افتح المتصفح على:
```
http://localhost:3000
```

---

## 📋 ما الذي يعمل الآن؟

### ✅ جاهز وفعّال:

1. **NOW SHOES Platform** 🟢
   - إدارة المنتجات
   - إدارة الطلبات
   - إدارة الشحنات
   - إدارة الموظفين

2. **نظام المصادقة** 🟢
   - تسجيل الدخول للموظفين
   - إدارة المستخدمين

3. **لوحة التحكم** 🟢
   - Dashboard للموظفين
   - Dashboard للمديرين
   - Dashboard للمؤسسين

### ⚠️ يحتاج إعداد إضافي:

1. **تكامل Shopify** 🟡
   - يحتاج: `SHOPIFY_ADMIN_API_TOKEN`
   - الملف: `apps/haderos-web/.env`

2. **تكامل Bosta (الشحن)** 🟡
   - يحتاج: `BOSTA_API_KEY`
   - الملف: `apps/haderos-web/.env`

3. **البريد الإلكتروني** 🟡
   - يحتاج: `SENDGRID_API_KEY`
   - الملف: `apps/haderos-web/.env`

### 🔴 معطّل حالياً:

1. **KAIA (محرك الشريعة)**
   - لتفعيله: `ENABLE_KAIA=true` في `.env`

2. **Sentinel (المراقبة)**
   - لتفعيله: `ENABLE_SENTINEL=true` في `.env`

3. **Blockchain**
   - لتفعيله: `ENABLE_BLOCKCHAIN=true` في `.env`

4. **AI Chat**
   - لتفعيله: `ENABLE_AI_CHAT=true` في `.env`
   - يحتاج: `OPENAI_API_KEY` أو `ANTHROPIC_API_KEY`

---

## 🛠️ الأوامر المتاحة

### في مجلد `apps/haderos-web`:

```bash
# التطوير (مع hot reload)
pnpm dev

# البناء للإنتاج
pnpm build

# تشغيل الإنتاج
pnpm start

# فحص الأخطاء
pnpm check

# الاختبارات
pnpm test

# تحديث قاعدة البيانات
pnpm db:push

# تشغيل المحاكاة (1500 عملية بيع)
pnpm simulate
```

---

## 🗄️ قاعدة البيانات

### معلومات الاتصال:
```
Host: localhost
Port: 5432
Database: haderos_dev
User: ahmedmohamedshawkyatta
```

### الجداول الموجودة (أهمها):
- `users` - المستخدمين
- `employees` - الموظفين
- `products` - المنتجات
- `orders` - الطلبات
- `shipments` - الشحنات
- `campaigns` - الحملات التسويقية
- `transactions` - المعاملات المالية

### الوصول إلى قاعدة البيانات:
```bash
psql -U ahmedmohamedshawkyatta -d haderos_dev
```

---

## 📂 هيكل المشروع

```
HADEROS-AI-CLOUD/
├── apps/
│   └── haderos-web/          ⭐ التطبيق الرئيسي
│       ├── client/           # React Frontend
│       ├── server/           # Node.js Backend
│       ├── drizzle/          # Database Schemas
│       └── .env             # متغيرات البيئة
│
├── services/
│   └── api-gateway/          # Python API (منفصل)
│
├── docs/                     # الوثائق
├── data/                     # البيانات
└── infrastructure/           # DevOps
```

---

## 🔧 حل المشاكل الشائعة

### 1. خطأ في الاتصال بقاعدة البيانات

```bash
# تحقق من PostgreSQL
pg_isready -h localhost -p 5432

# إذا كان متوقفاً
brew services start postgresql@15
```

### 2. Port 3000 مشغول

التطبيق سيختار port آخر تلقائياً (3001, 3002, إلخ)

### 3. أخطاء في التثبيت

```bash
# امسح node_modules وأعد التثبيت
rm -rf node_modules
pnpm install
```

### 4. قاعدة البيانات لا تحتوي على جداول

التطبيق سينشئ الجداول الأساسية تلقائياً عند التشغيل الأول.

---

## 🎯 الخطوات التالية

### للتطوير الفوري:

1. ✅ تشغيل التطبيق محلياً
2. ✅ اختبار الـ Dashboard
3. ⏳ إضافة منتجات جديدة
4. ⏳ اختبار نظام الطلبات

### للتكامل الكامل:

1. ⏳ الحصول على Shopify API Token
2. ⏳ الحصول على Bosta API Key
3. ⏳ إعداد SendGrid للبريد الإلكتروني
4. ⏳ تفعيل الوحدات المتقدمة (اختياري)

### للنشر:

1. ⏳ مراجعة ملف `.env.production`
2. ⏳ بناء التطبيق: `pnpm build`
3. ⏳ النشر على DigitalOcean أو AWS
4. ⏳ إعداد Domain و SSL

---

## 📞 الدعم

### الوثائق:
- `docs/` - الوثائق الكاملة
- `README.md` - نظرة عامة

### الملفات المهمة:
- `DEPLOYMENT_STATUS.md` - حالة النشر
- `HANDOVER_REPORT.md` - تقرير التسليم
- `QUICK_START_GUIDE.md` - دليل البدء السريع

### الأسئلة الشائعة:

**س: هل يعمل المشروع محلياً؟**
ج: نعم ✅ - اتبع الخطوات أعلاه

**س: هل أحتاج Python؟**
ج: لا - التطبيق الرئيسي يعمل بـ Node.js فقط

**س: متى أستخدم بوابة Python API؟**
ج: عند تفعيل الوحدات المتقدمة (KAIA, Blockchain, إلخ)

**س: كيف أضيف منتجات؟**
ج: من Dashboard → Products → Add New

**س: هل البيانات محفوظة؟**
ج: نعم - كل شيء في PostgreSQL

---

## ⚡ نصيحة سريعة

**للمطور الجديد:**
- ابدأ بتشغيل التطبيق أولاً ✅
- لا تقلق من الوحدات المعقدة
- ركز على NOW SHOES Platform
- التكاملات الخارجية اختيارية

**للتسليم:**
- تأكد من تشغيل التطبيق محلياً ✅
- وثّق أي مفاتيح API مطلوبة 📝
- اشرح ما يعمل وما لا يعمل 📊
- اترك خارطة طريق واضحة 🗺️

---

**🎉 جاهز للانطلاق! ابدأ الآن بـ `pnpm dev` في مجلد `apps/haderos-web`**
