# 🔄 نظام النسخ الاحتياطي التلقائي - HADEROS

## 📋 نظرة عامة

هذا النظام ينسخ تلقائياً جميع الملفات الجديدة والمعدلة إلى:
1. **النسخة المحلية** (git commit)
2. **المستودع الرئيسي** (origin: HADEROS-AI-CLOUD)
3. **المستودع الاحتياطي** (backup: new-one-test)

---

## 🚀 الاستخدام السريع

### النسخ الاحتياطي اليدوي (مرة واحدة)

```bash
./scripts/auto-backup.sh
```

### النسخ الاحتياطي التلقائي (مراقبة مستمرة)

```bash
./scripts/watch-and-backup.sh
```

---

## 📝 التفاصيل

### 1. `auto-backup.sh`
سكريبت النسخ الاحتياطي الأساسي:
- ✅ يفحص الملفات الجديدة والمعدلة
- ✅ يضيفها إلى git
- ✅ ينشئ commit تلقائياً
- ✅ يرفع إلى origin (المستودع الرئيسي)
- ✅ يرفع إلى backup (المستودع الاحتياطي)

**الاستخدام:**
```bash
cd /Users/ahmedmohamedshawkyatta/Documents/HADEROS-AI-CLOUD
./scripts/auto-backup.sh
```

### 2. `watch-and-backup.sh`
سكريبت المراقبة التلقائية:
- ✅ يراقب التغييرات في الملفات
- ✅ يرفع تلقائياً عند أي تغيير
- ✅ يعمل في الخلفية

**الاستخدام:**
```bash
cd /Users/ahmedmohamedshawkyatta/Documents/HADEROS-AI-CLOUD
./scripts/watch-and-backup.sh
```

**للإيقاف:** اضغط `Ctrl+C`

### 3. GitHub Actions Workflow
نسخ احتياطي تلقائي عند كل push:
- ✅ يعمل تلقائياً عند push إلى main/master
- ✅ ينسخ إلى المستودع الاحتياطي
- ✅ لا يحتاج تدخل يدوي

**الموقع:** `.github/workflows/auto-backup.yml`

---

## 🔧 الإعداد الأولي

### 1. إضافة Remote Backup (تم بالفعل)
```bash
git remote add backup https://github.com/ka364/new-one-test.git
```

### 2. التحقق من الـ Remotes
```bash
git remote -v
```

يجب أن ترى:
```
backup    https://github.com/ka364/new-one-test.git (fetch)
backup    https://github.com/ka364/new-one-test.git (push)
origin    git@github.com:ka364/HADEROS-AI-CLOUD.git (fetch)
origin    git@github.com:ka364/HADEROS-AI-CLOUD.git (push)
```

### 3. تثبيت fswatch (اختياري - للمراقبة التلقائية)
```bash
brew install fswatch
```

---

## 📊 ما يتم نسخه؟

### ✅ يتم نسخه:
- جميع الملفات الجديدة (`??`)
- جميع الملفات المعدلة (`M`)
- جميع الملفات المحذوفة (`D`)

### ❌ لا يتم نسخه (في .gitignore):
- `node_modules/`
- `.env` files
- `dist/`, `build/`
- `.DS_Store`
- Logs

---

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: نسخ احتياطي بعد تعديل ملفات
```bash
# بعد تعديل أي ملفات
./scripts/auto-backup.sh
```

### السيناريو 2: نسخ احتياطي تلقائي مستمر
```bash
# في terminal منفصل
./scripts/watch-and-backup.sh

# الآن أي تعديل سيُرفع تلقائياً!
```

### السيناريو 3: نسخ احتياطي يدوي مع رسالة مخصصة
```bash
git add -A
git commit -m "رسالة مخصصة"
git push origin main
git push backup main
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: "remote backup already exists"
```bash
# الحل: تحديث الـ URL
git remote set-url backup https://github.com/ka364/new-one-test.git
```

### المشكلة: "Permission denied"
```bash
# الحل: تأكد من صلاحيات GitHub
# أو استخدم SSH بدلاً من HTTPS:
git remote set-url backup git@github.com:ka364/new-one-test.git
```

### المشكلة: "fswatch not found"
```bash
# الحل: تثبيت fswatch
brew install fswatch

# أو استخدم وضع polling (أبطأ لكن يعمل)
# السكريبت سيكتشف تلقائياً
```

---

## 📈 الإحصائيات

بعد كل نسخ احتياطي، سترى:
- عدد الملفات الجديدة
- عدد الملفات المعدلة
- رسالة الـ Commit
- حالة الرفع (نجح/فشل)

---

## ✅ التحقق من النسخ الاحتياطي

### التحقق من المستودع المحلي:
```bash
git log --oneline -5
```

### التحقق من المستودع الرئيسي:
```bash
# زيارة: https://github.com/ka364/HADEROS-AI-CLOUD
```

### التحقق من المستودع الاحتياطي:
```bash
# زيارة: https://github.com/ka364/new-one-test
```

---

## 🎉 الخلاصة

الآن لديك **3 مستويات من النسخ الاحتياطي**:
1. ✅ **محلي** - git commit
2. ✅ **رئيسي** - origin repository
3. ✅ **احتياطي** - backup repository

**أي ملفات تعملها = نسختين تلقائياً!** 🚀

---

**آخر تحديث:** 29 ديسمبر 2025

