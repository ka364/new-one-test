# 🔍 دليل البحث البصري - NOW SHOES

## نظرة عامة

نظام البحث البصري يتيح للموظفين والعملاء البحث عن المنتجات باستخدام الصور بدلاً من النصوص. النظام يستخدم تقنيات الذكاء الاصطناعي (AI Vision) لتحليل الصور والعثور على منتجات مشابهة.

---

## 📋 المكونات الأساسية

### 1. قاعدة البيانات
- **product_images**: تخزين صور المنتجات
- **image_embeddings**: تخزين البصمات الرقمية للصور
- **visual_search_history**: سجل عمليات البحث
- **product_barcodes**: ربط الباركود بالمنتجات

### 2. APIs
- `visualSearch.searchByImage`: البحث بالصورة
- `visualSearch.generateEmbeddings`: توليد embeddings
- `visualSearch.getHistory`: عرض تاريخ البحث
- `visualSearch.provideFeedback`: تقييم النتائج

### 3. الواجهات
- `/visual-search`: صفحة البحث البصري (للجميع)
- `/product-images`: إدارة صور المنتجات (للموظفين)

---

## 🚀 كيفية الاستخدام

### للموظفين: رفع صور المنتجات

1. **الدخول إلى صفحة إدارة الصور**
   - افتح: `https://your-domain.com/product-images`
   - تسجيل الدخول مطلوب

2. **رفع الصور**
   - أدخل كود المنتج (مثال: AMA8, PR20)
   - اختر صورة أو عدة صور
   - اضغط "رفع الصور"
   - سيتم توليد embeddings تلقائياً

3. **نصائح للصور**
   - استخدم صور واضحة وعالية الجودة
   - خلفية بيضاء أو محايدة
   - المنتج يظهر بالكامل
   - عدة زوايا لنفس المنتج

### للعملاء: البحث بالصورة

1. **الدخول إلى صفحة البحث**
   - افتح: `https://your-domain.com/visual-search`

2. **طرق البحث**
   - **التقاط صورة**: استخدم الكاميرا مباشرة
   - **رفع صورة**: اختر صورة من جهازك
   - **مسح باركود**: امسح QR أو باركود

3. **النتائج**
   - ستظهر المنتجات المشابهة
   - مرتبة حسب نسبة التطابق
   - يمكن عرض التفاصيل أو إضافة لطلب

---

## 🔧 الإعدادات التقنية

### متغيرات البيئة المطلوبة
```env
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your-api-key
DATABASE_URL=your-database-url
```

### التبعيات
- OpenAI Vision API (مدمج في Manus)
- S3 Storage (مدمج في Manus)
- MySQL/TiDB Database

---

## 📊 كيف يعمل النظام؟

### 1. رفع الصور
```
صورة المنتج → رفع إلى S3 → حفظ URL في DB
```

### 2. توليد Embeddings
```
صورة → OpenAI Vision API → Vector (512 dimensions) → حفظ في DB
```

### 3. البحث
```
صورة البحث → توليد embedding → مقارنة مع جميع embeddings → ترتيب حسب التشابه → عرض النتائج
```

### 4. حساب التشابه
يستخدم النظام **Cosine Similarity** لحساب التشابه بين الصور:
- 0.9 - 1.0: تطابق عالي جداً
- 0.7 - 0.9: تطابق عالي
- 0.5 - 0.7: تطابق متوسط
- أقل من 0.5: تطابق ضعيف

---

## 🎯 الخطوات التالية

### المرحلة الأولى (الإطلاق)
- [x] بناء البنية التحتية
- [x] إنشاء الواجهات
- [x] استيراد المنتجات
- [ ] رفع صور المنتجات
- [ ] توليد embeddings
- [ ] اختبار النظام

### المرحلة الثانية (التحسين)
- [ ] تحسين دقة البحث
- [ ] إضافة فلاتر (اللون، المقاس، السعر)
- [ ] تقارير وإحصائيات
- [ ] تكامل مع نظام الطلبات

### المرحلة الثالثة (التطوير)
- [ ] البحث بالصوت
- [ ] التوصيات الذكية
- [ ] AR Try-On (تجربة افتراضية)

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تظهر نتائج البحث
**الحل:**
1. تأكد من وجود صور للمنتجات في قاعدة البيانات
2. تأكد من توليد embeddings للصور
3. تحقق من جودة صورة البحث

### المشكلة: نتائج غير دقيقة
**الحل:**
1. ارفع صور أوضح للمنتجات
2. ارفع عدة صور من زوايا مختلفة
3. استخدم خلفية بيضاء أو محايدة

### المشكلة: بطء في البحث
**الحل:**
1. تحقق من سرعة الإنترنت
2. قلل حجم صورة البحث
3. تحقق من عدد المنتجات في قاعدة البيانات

---

## 📞 الدعم الفني

للمساعدة أو الاستفسارات:
- Email: support@haderos.ai
- Website: https://haderos.ai

---

## 📝 ملاحظات مهمة

1. **الخصوصية**: جميع الصور محفوظة بشكل آمن على S3
2. **الأداء**: يستغرق توليد embedding حوالي 2-3 ثواني لكل صورة
3. **التكلفة**: كل عملية بحث تستهلك ~$0.001 من OpenAI API
4. **الحد الأقصى**: 5 ميجابايت لكل صورة

---

تم إنشاء هذا الدليل بواسطة HaderOS AI 🤖
آخر تحديث: ديسمبر 2025
