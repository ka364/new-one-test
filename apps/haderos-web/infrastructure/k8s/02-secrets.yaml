apiVersion: v1
kind: Secret
metadata:
  name: haderos-secrets
  labels:
    app: haderos
type: Opaque
stringData:
  # WARNING: In real production, use SealedSecrets or Vault.
  # These are base64 encoded automatically when applied if using stringData
  POSTGRES_PASSWORD: "haderos_secure_2024"
  JWT_SECRET: "change-this-in-production-extremely-secure-key"
  OPENAI_API_KEY: "change-me"
  # Database URL constructed for App access
  DATABASE_URL: "postgresql://haderos:haderos_secure_2024@haderos-postgres:5432/haderos_platform"
  REDIS_URL: "redis://haderos-redis:6379/0"
